# Stellara Work model API keys (main-process only, safeStorage-encrypted)
# Format: STELLARA_KEY_<modelId>=<your-key>
# Example:
#   STELLARA_KEY_deepseek-v4-pro=sk-...
# Key files are NEVER committed. See docs/macos-migration.md for data locations.

# ---------------------------------------------------------------------------
# 云账号（Phase 3 · 腾讯云 CloudBase）— 可选功能
# ---------------------------------------------------------------------------
# 不配置时「设置 → 账号 → 云账号」会提示「尚未配置云端环境」，
# 本地身份（Phase 1）与所有其他功能完全不受影响。
#
# 取值：CloudBase 控制台 →「API Key 配置」中的 **Publishable Key**
#   https://tcb.cloud.tencent.com/dev?envId=stellara-prod-d0g594tmi6e977e10#/env/apikey
#
# ⚠️ 只能填 Publishable Key（官方定位为可安全暴露于客户端）。
#    绝不要填 API Key —— 那是管理员权限的服务端凭证。
#
# 前置条件（控制台侧一次性配置）：
#   1. 身份认证 → 登录方式 → 开启「邮箱验证码」
#   2. 身份认证 → 登录方式 → 邮箱，配置发件渠道（腾讯云邮件 或 自建 SMTP）
#
# STELLARA_CLOUDBASE_PUBLISHABLE_KEY=
