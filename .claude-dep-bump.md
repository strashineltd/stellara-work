# Dependency Security Bump — hono / qs

Date: 2026-09-13
Branch: main
Mode: controlled update (no `npm audit fix --force`)

## Before → After

| Package | Before | After | Fixed-in range | Change |
|---------|--------|-------|----------------|--------|
| hono | 4.13.1 | 4.13.7 | >= 4.13.5 | `npm update` (within `^4.11.4`) |
| qs   | 6.15.3 | 6.16.0 | >= 6.16.0 | `npm update` (within `^6.12.3`/`^6.14.0`/`^6.15.2`) |

Both are transitive, not direct deps. No `overrides` entries were needed; `npm update hono qs --legacy-peer-deps` moved both within semver. `package.json` unchanged; only `package-lock.json` modified.

## Dependency chains

- `hono` ← `@modelcontextprotocol/sdk@1.30.0` (direct), `@hono/node-server@2.1.0`
- `qs` ← `express@5.2.1`, `body-parser@2.3.0`, `url@0.11.4` (`@cloudbase/js-sdk@3.9.3` → `@cloudbase/signature-nodejs@2.0.0`)

## Advisory IDs (production audit, before)

hono (3 moderates, all fixed in 4.13.5):
- GHSA-gqvv-2mrq-wpjv — Incomplete fix for CVE-2026-39408: `toSSG()` still writes files outside output dir (CWE-22, CVSS 6.5)
- GHSA-g6gw-c38x-mqfc — Unbounded dot-notation nesting in `parseBody()` → memory exhaustion (CWE-400, CVSS 5.3)
- GHSA-crvj-82cr-hjcx — Query parser reads params after URL fragment; cache-key/proxy differentials (CWE-444, CVSS 5.9)

qs (2 moderates, both fixed in 6.16.0):
- GHSA-4mjr-xmp4-gh2g — DoS via attacker-controlled isBuffer (CWE-248/703, CVSS 5.3; range >=2.2.5 <6.16.0)
- GHSA-x5fp-wj9c-mxmx — array-limit bypass via bracket-key comma parsing (CWE-770, CVSS 3.7; range >=6.14.2 <=6.15.3)

## Audit summary

- Before: `{ info: 0, low: 0, moderate: 2, high: 0, critical: 0, total: 2 }`
- After: `{ info: 0, low: 0, moderate: 0, high: 0, critical: 0, total: 0 }`

Remaining production advisories: none.

Audit note: configured mirror `registry.npmmirror.com` lacks the audit endpoint (404 `NOT_IMPLEMENTED`); audits were run with `--registry=https://registry.npmjs.org`. Installs/updates used the mirror (lockfile remains consistent, mirror had the patched versions).

## Verification

- `npm run typecheck` — exit 0
- `npm test` — exit 0, tail:

```
 Test Files  135 passed | 1 skipped (136)
      Tests  1937 passed | 2 skipped (1939)
   Duration  83.95s
```

Baseline (1937 passed / 2 skipped) matched exactly.

- `npm run build` — exit 0, tail:

```
dist/assets/index-RWYc2N3E.js   1,189.86 kB │ gzip: 380.71 kB
✓ built in 297ms
(!) Some chunks are larger than 500 kB after minification. ...
> stellara-work@0.9.3 build:electron
> node -e "..." && tsc -p electron/tsconfig.json
```

(The >500 kB chunk warning is pre-existing, unrelated to this bump.)

## Concerns

- None blocking. The mirror/npmjs registry split was already the repo's status quo for audit-only calls; the mirror served the patched versions.
- The build output was cleaned/rebuilt by `build:electron` as part of `npm run build` (expected).
