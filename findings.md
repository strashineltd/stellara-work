# Findings

## 2026-08-02 Project-scoped workspace and standard app storage

- 当前默认配置本身不会生成项目，但首次 onboarding 会强制选择模型级 `workDir`，完成后还会自动创建一个未分组会话；这与“默认不设置工作项目”冲突。
- 项目创建窗口目前把 `config.workDir` 作为唯一文件根目录，因此取消全局目录后两个文件动作会全部禁用；需要由原生文件选择/保存动作直接产生项目目录。
- 会话创建一律复制活跃模型的 `workDir`，没有按 `projectId` 读取项目目录；应由主进程以项目记录为准，避免 renderer 传入不一致目录。
- 文件 IPC 的授权清单只包含模型目录和应用默认目录，尚未包含数据库中的项目目录；项目化后必须把项目/会话目录纳入只读授权来源，并保留原生选择产生的临时授权。
- 精确保存“用户设置的文件”需要给项目增加 `entryFile` 字段；只存父目录会导致重启后入口文件丢失，不能满足项目窗口的持久化语义。
- 配置、密钥、环境文件与 SQLite 当前分散硬编码到 `~/.stellara`。Electron 已提供 `app.getPath('userData')` 作为标准可写应用数据目录；安装目录/ASAR 不适合写入可变数据，普通安装权限和升级替换都会造成失败或丢失。
- 采用标准 userData 时需要在 app ready、任何配置/数据库访问之前设置运行时目录，并按“目标不存在才复制”的方式迁移旧 `~/.stellara` 文件，保留旧目录作为可回退副本。
- 本轮设计系统搜索误判为作品集/移动端并建议琥珀 CTA、Lora/Raleway，与既有 Calm Workspace 和桌面参考图冲突；只采用其可见焦点、150–300ms、reduced-motion 和明确禁用状态要求。
- 已建立单一运行时数据目录模块；Electron ready 后先设置 `app.getPath('userData')`、迁移缺失的旧文件，再加载 `.env`、配置和 SQLite。迁移保留旧目录且不覆盖新目录已有文件。
- 项目新增 `entry_file` 无损迁移；创建/编辑项目的原生文件选择会授予文件父目录，后续项目写库和文件 IPC仍需通过主进程授权校验。
- 项目会话的执行目录优先取项目记录，再兼容旧会话/旧模型目录，因此更换项目入口文件后现有会话不会继续跑在旧目录。
- 首次模型配置不再要求目录，也不再自动创建会话；无当前项目时“新建会话/开始任务”转到项目选择，只有空项目列表才自动打开创建窗口。
- 真实预览确认：创建窗口无全局目录也能选择/新建文件，未选入口文件时最终按钮禁用；选中文件后显示相对路径并可创建一次；应用设置不再出现默认工作目录，只显示标准数据目录说明。
- Windows 打包目录存在管理员所有权时，electron-builder 的最终 rename 会报 `EPERM`；本轮通过把已验证的 Electron 基础运行时移到精确临时路径并作为 `electronDist` 输入，让 builder 重新注入正式 `app.asar`，避免把 `--prepackaged` 生成的默认 Electron 壳误当成应用安装包。

- 当前工作区已有大量用户未提交的功能改动；只修改本次 P0 流程直接涉及的文件。
- 既有检查确认：主进程默认自动批准危险操作；preload 暴露 abort/approve，但主进程尚未处理。
- 当前代码已有一批进行中的 P0 改动：主进程已增加 abort handler、approval resolver 和工作目录必填检查；后续修改必须基于这些改动增量完成。
- 现有实现仍有关键缺口：审批 resolver 未关联 stream，finally 会拒绝所有 stream 的待审批；abort 不会唤醒正在等待审批的 Promise；chat:start 仍按全局 active model 启动，未按 session 绑定模型/工作目录。
- 已修复的 P0 增量：审批 registry 现按 stream 隔离并支持超时/取消；chat:start 强制 sessionId，按 session → project → model 优先级绑定模型和工作目录；renderer 在结束或取消时清理审批条。
- 验证环境：renderer build 成功；Electron TypeScript build 被 `electron/dist` 中既有 git/memory 输出文件的 Windows 访问锁阻断（TS5033），未尝试删除或覆盖这些用户产物。
- UI 基线：`global.css` 约 2792 行，现有 tokens 已接近 Linear 风格但侧栏黑白反差和文案仍偏 Agent 聊天；主组件已拆分为 Header、Sidebar、ChatStream、InputArea、WorkspacePanel、ApprovalTopBar，可在保留业务逻辑的前提下重构。
- UI 设计工具给出了 OLED/粉色/glow 建议，与用户明确的“无 AI 味和科幻感”冲突；本轮采用其可访问性、焦点、密度和反馈规范，但视觉改为浅色暖灰 + 静蓝 Quiet Workbench。
- 现有 UI 测试主要绑定组件 class 和可访问行为，只有 Onboarding 测试绑定 `Welcome to Stellara` 文案；主工作台可以更新中文微文案，但引导页标题需保留兼容文本或同步测试。
- `global.css` 已超过 3000 行且包含历史分区；本轮保留现有选择器并在文件末尾集中加入 Quiet Workbench 覆盖层，降低误删功能样式的风险。tokens 文件则作为唯一基础色与尺寸来源直接重写。
- 辅助界面仍散落着文件夹、警告、成功等 emoji 字符；这些不是业务依赖，统一替换为同一套 16px 线性 SVG 图标，以避免界面出现聊天机器人式的视觉碎片。
- renderer 生产构建成功，但主 JS chunk 约 1.0 MB，超过 Vite 的 500 kB 建议阈值；这是后续可单独处理的代码分包与首屏性能项，不影响本轮 UI 交付。
- 全量测试中 9 个符号链接用例在 fixture 创建阶段因 Windows `EPERM` 失败；UI 组件套件 49/49 通过。仓库的 `npm run lint` 还依赖旧版 ESLint 配置方式，而当前 ESLint 10 要求 `eslint.config.*`。
- 本轮回归修复遵循：图标保持统一 16px / 1.5px stroke 并为图标按钮保留文字或 aria-label；设置导航只保留一个稳定入口；切换动效使用 150–220ms 的 opacity/transform，不动画 width/height，并继续尊重 reduced-motion。
- 设计检索再次给出夸张极简/青橙配色，不符合已确定的 Quiet Workbench，继续沿用现有暖灰静蓝视觉；采纳其“预留固定内容空间、入场 ease-out、避免线性与宽高动画、reduced-motion”的交互建议。
- 重复设置入口已定位：侧栏底部一个，Header 溢出菜单同时包含“重新配置”和“设置”；命令面板与错误恢复中的设置属于按需入口，不在常驻导航中。修复方向是 Header 常驻齿轮为唯一常驻入口，移除侧栏与溢出菜单重复项。
- 设置窗口跳动根因已定位：CSS 只有 `max-height` 而没有稳定 `height`，不同 tab 的内容高度直接改变整个 modal；设置内容也没有独立的切换容器/key，因此只能瞬时替换。
- 共享 `Icon` 当前没有统一 class/style，SVG 仍按 inline baseline 参与布局；图标按钮也没有统一 `line-height`/`flex-shrink` 约束。这会导致部分位置出现垂直偏移、压缩或看似尺寸错误。
- 侧栏旧设置按钮通过 `stellara:open-settings` 自定义事件触发，但 renderer 内没有对应监听器，因此它不仅重复，而且实际可能无响应；移除后由 Header 直接调用 `onOpenSettings`，路径更短且可测试。
- 第二轮设计系统检索仍误判为内容营销页/夸张极简深色方案，不适合桌面开发工作台；不采用其超大字体、纯深色和绿色 CTA，只保留 Inter/系统字体、清晰焦点、150–300ms 状态过渡、深浅主题对比和多断点检查原则。
- 会话恢复脚本未报告未同步上下文；当前精修可直接基于已完成的 Quiet Workbench 与 UI 回归修复继续。
- 主工作台结构已有清晰的 Header / Sidebar / Chat / Input / Workspace 分区，但第二轮审查发现若干“看起来能点、键盘却不能操作”的细节：侧栏会话与项目头使用可点击 `li/div`，Tab 关闭使用可点击 `span`，工作区计划步骤同样缺少按钮语义。
- 侧栏搜索过滤的 `useMemo` 内调用 `setExpanded`，会在 render 期间更新状态；应移动到 effect，避免搜索时的 React 渲染警告和潜在闪动。
- TabBar 仍用 `●/◐/◯/+ /×` 字符承担结构图标，和已经建立的统一 SVG 图标语言不一致；关闭、新建、状态指示需要无布局抖动的可访问实现。
- 确认清空任务的弹窗缺少 `role="dialog"`、标题关联和 Escape 关闭；工作区进度条缺少 progressbar 语义，局部加载/错误文案仍混用英文省略号和 `Error:`。
- 输入区已有明确主动作和计划模式，但 slash 建议项仍是可点击 `div`，缺少 listbox/option 语义；主交互可以在不改业务数据流的前提下完善键盘焦点和状态反馈。
- 当前样式采用“历史基础 + 文件末尾 Quiet Workbench 覆盖层”，同一选择器存在多处定义；本轮继续在末尾增加集中、命名清楚的 polish layer，避免大范围删除旧样式影响未覆盖功能。
- 设计令牌已有完整的暖灰/静蓝深浅主题、4px 间距、圆角和动效节奏，可直接扩展控件高度、层级阴影与状态令牌，不需要引入新色板或外部字体。
- 主界面在 1180/980px 仅压缩侧栏和右侧工作区，缺少更窄窗口下的主工作台策略；设置页已有 760px 横向标签适配，但 Header、Sidebar、Workspace 在约 840px 以下仍可能挤压聊天区。
- 旧的 sidebar footer/settings 样式仍留在 CSS 中但已无对应 JSX；它们不产生可见重复入口，本轮不把无害的历史清理混入交互精修。
- Slash 技能菜单存在真实交互缺陷：输入 `/` 时 `handleChange` 只引用了 `onSlashClose` 而未调用打开逻辑，父组件也没有暴露 open 回调，因此自动补全实际上无法出现；本轮将补齐 `onSlashOpen` 并增加测试。
- 现有组件测试主要验证 class、基础点击与无 emoji，可在保持原契约的同时增加键盘操作、语义角色和 slash 菜单打开的回归覆盖。
- 项目没有 MainView 专用测试，但 InputArea、Sidebar、TabBar、SettingsModal、WorkspacePanel 都有轻量 jsdom 契约测试；新增交互覆盖应放在这些组件测试中，避免为了 UI 精修引入大型 Electron 集成夹具。
- 生产入口依赖 preload 的 `window.electronAPI` 和本地模型/会话数据，直接用普通浏览器打开 Vite 页面不能代表真实 UI；本轮视觉收口以组件结构、响应式 CSS、深浅主题令牌和生产构建为主，不引入只为截图存在的 mock 应用层。
- UI 规范的交付前检索再次强调可见焦点、150–300ms 动效、明确加载反馈、系统化 z-index 和避免横向溢出；本轮新增的 focus ring、motion token、状态文案、z-index token 与 860px 覆盖面板策略符合这些要求。
- 对比度计算结果：浅色主文字 14.60:1、soft 5.06:1，深色主文字 12.61:1、soft 6.51:1，均满足普通文本 AA；浅色 `text-faint` 只有 3.13:1，却被用于 10–11px 元数据，需要调深到至少 4.5:1。深色 faint 为 4.13:1，也需小幅提亮以覆盖小字号文本。
- 深色主题的 `--color-primary` 是浅色前景，但品牌方块同时使用了浅色 `text-on-dark`，存在浅底浅字；深色 accent/danger 按钮也不应继续固定使用白字。已新增 on-accent/on-danger/on-success 令牌并修正品牌标记和状态按钮的主题内对比。
- `text-faint` 已调整为浅色 `#777770`（白底 4.51:1）和深色 `#8f8f87`（内容底 4.71:1），保留与 soft 文本的层级差同时满足小字号 AA。
- 新任务要求移除 Header 左上角 `Stellara Work` 文本和方块标记，仅保留会话侧栏开关；不得影响 Onboarding 或安装包产品名。
- 项目重命名/删除需要同时核对可发现入口、renderer 状态更新和主进程 IPC/数据库行为，不能只做表面菜单调整。
- 会话恢复脚本未报告未同步内容，可在当前已验证的 61 项 UI 测试基线上继续。
- 本轮设计检索的深色/绿色视觉仍与 Quiet Workbench 不符，不采用；其针对项目操作的有效要求是：危险删除必须确认、失败需就地且可被读屏宣布、操作不能只依赖右键、成功/失败状态要明确。
- 项目 rename/delete 当前在 `MainView` 中使用 fire-and-forget IPC，并在请求完成前立即更新 renderer 状态；IPC 失败会被完全吞掉，用户既看不到原因，界面状态还可能与数据库不一致。
- Sidebar 的项目重命名/删除只藏在右键菜单里，没有可见操作按钮；这使多数用户无法发现操作，也不满足键盘和非鼠标交互要求。
- 新建数据库的 `sessions.project_id` 有 `ON DELETE SET NULL`，但旧数据库是通过 `ALTER TABLE ... ADD COLUMN project_id` 迁移的，SQLite 无法在该迁移中补外键；因此旧库删除项目后可能留下孤立 projectId，会话重启后既不在项目组也不在“未分组”中。
- `deleteProject`/`renameProject` 当前不检查 SQLite `changes`，不存在的项目也会静默成功；IPC 同样不校验 id/name。修复应使用事务显式解绑 sessions、校验影响行数，并把失败传回 renderer。
- Sidebar 测试目前没有任何项目数据，也没有覆盖项目菜单、重命名提交或删除确认；需要新增专门的项目交互用例。
- Header 现有测试不依赖品牌文字，可直接移除 `.header-product` 与 `.header-divider` JSX，并新增“主工作台不再渲染品牌区”的契约测试。
- 项目数据库已有独立 `db.test.ts` 和 `_setDbPath` 隔离钩子；可在现有临时 SQLite 测试中补充重命名、删除解绑会话、缺失项目报错，无需引入新的测试基础设施。
- 修复方案确定为：项目行增加可见的 `more` 按钮；重命名/删除改为 await IPC 后再更新 UI；删除前明确确认；Sidebar 就地显示 success/error live feedback；数据库事务显式解绑 sessions 并校验影响行数。
- 交付前 UX 检索确认本轮实现覆盖高优先级要求：删除确认、异步期间禁用、成功提示、失败 alert、键盘可达、统一 z-index/focus；没有引入额外装饰性动画。
- 静态检查确认 `Header.tsx` 已无 `header-product`、品牌方块或 `Stellara Work` 文本；项目操作入口、内联面板、反馈区以及 MainView 的 await mutation 均已存在。
- 工作区仍包含大量此前用户改动，本轮只增量修改 Header、Sidebar、项目 IPC/数据库及其测试，不整理或覆盖无关差异。
- 用户实际旧库报错 `no such column: project_id` 的根因已确认：`getDb()` 的首个批量 schema `exec` 在旧 `sessions` 表上先执行 `CREATE INDEX ... sessions(project_id)`，因此在第 120 行的 `ALTER TABLE ... ADD COLUMN project_id` 迁移之前就抛错。主进程捕获初始化错误后继续启动，而 `_db` 已被缓存，后续删除项目直接命中缺列错误。
- 修复必须把 `idx_sessions_project` 的创建移到列迁移之后，并用 `PRAGMA table_info(sessions)` 明确判断字段是否存在，避免捕获并吞掉所有 `ALTER TABLE` 错误；同时用真实旧表结构增加回归测试。
- 修复后的旧库回归验证通过：初始化会识别缺失字段、执行无损 `ALTER TABLE`、随后创建索引；旧会话可关联项目，删除项目后 `project_id` 被显式清空且会话仍存在。
- 2026-07-31 从零 UI 任务的产品定义：Electron 桌面生产力 / 开发工作台，面向长时间处理项目、会话与本地文件的个人用户；目标气质为 grounded、editorial、quiet、tactile、content-first。
- `ui-ux-pro-max` 设计系统检索仍推荐 OLED 黑底、粉色 CTA 与 glow，和用户“不要科幻风、不要一眼 AI”的明确要求冲突，因此拒绝其具体视觉方案；仅采用语义 tokens、统一 SVG、可见焦点、响应式、150–300ms 状态动效和 reduced-motion 规范。
- 当前渲染层结构清晰但视觉债务集中在样式层：主流程由 `MainView / Sidebar / Header / TabBar / ChatStream / InputArea / WorkspacePanel` 组成，设置与引导独立；业务状态可以保留，布局与视觉可重做。
- `global.css` 已达到 5434 行，包含多轮追加覆盖，继续叠加会让层级难以推理。此次“从零设计”应改为重写 tokens 与全局样式，而不是再追加一层覆盖；保留现有 class 契约可降低业务回归风险。
- 主界面现有信息架构是左侧项目/会话、中部标签与任务记录、右侧工作区详情，适合桌面生产力产品；本轮重点改变密度、视觉层级、导航语义和空状态，不把它伪装成聊天机器人或仪表盘。
- 顶栏功能本身合理，但当前“工作目录 + 模型 + 5 个图标动作”权重接近；新设计应把工作目录作为位置线索、模型作为次级控件，将设置/文件/详情操作统一为一组安静的工具按钮。
- 中部可以从聊天气泡语义转成任务文档：用户输入显示为任务摘要块，系统输出显示为连续工作日志；输入区使用底部浮动编辑器但不做玻璃、光晕或巨型圆角。
- Sidebar 已具备项目树、搜索、项目 CRUD 与反馈，可通过信息层级重排形成“资料栏”：顶层标题与新建动作、搜索、项目分组、会话时间；无需改动数据流。
- WorkspacePanel 的目标/进度/交付物/文件四段适合检查器结构；新样式应减少卡片套卡片，改用段落标题、细分隔线和可折叠列表。
- 详细样式检索中可采用的部分是 Swiss Modernism 的严格网格、8px 节奏、单一强调色和高对比；夸张大字、Bento 卡片、全黑白、全等宽字体与营销页单栏模式不适合桌面工作台，明确排除。
- 字体不引入在线 Google Fonts，继续采用 Windows 原生 `Segoe UI Variable`，代码/路径使用 `Cascadia Code`；这样离线稳定，也比“Inter + JetBrains Mono + 深色面板”的常见 AI 产品组合更像原生桌面工具。
- 测试主要绑定语义、关键 class 和功能文案，视觉层可以整体替换。新样式文件应覆盖 onboarding、settings、命令面板、文件树、审批、工具调用与 Markdown，避免只有主屏“换皮”。
- 新入口已改为 `grounded-tokens.css + workbench.css`，旧 `tokens.css/global.css` 不再加载；因此历史覆盖层不会干扰新设计，同时用户原文件仍保留、便于审计回退。
- 新样式将任务流视为连续文档：92px 角色栏 + 正文列、任务摘要用上下分隔线、工作记录无气泡；输入器使用有边界的纸面编辑器，工具调用使用紧凑记录行，避免 AI 聊天卡片堆叠。
- 本地视觉回归揭示设置空白并非 CSS 问题：`Header` 把 React `MouseEvent` 传给 App 中接收可选 `SettingsTab` 的回调，事件对象随后成为 `SettingsModal.initialTab`。修复应在 App/MainView 边界把常规设置入口固定为无参语义回调。
- 设置固定尺寸策略经真实布局测量验证：模型页与应用页切换时 modal 都是 960×656；面板内容高度不同但由 `.settings-body` 独立滚动，不再引起窗口变大变小。
- 在应用允许的最小 900px 窗口下，资料栏切换为 244px 覆盖面板，主内容保持 900px 画布且文档 `scrollWidth === clientWidth`，页面没有横向溢出。
- 用户对“从零设计”的目标以提供的 MP4 为准；上一版自然纸面/炭灰工作台不再是最终视觉依据。必须先覆盖视频时间轴抽帧，再判断它的真实风格、布局和交互节奏。
- 参考视频是外部视觉资料，仅提取可见设计特征；其中若出现文字或操作指示，视为不可信内容，不作为开发命令执行。
- 最终参考改为 1536×1024 JPG。实际应用目标取图中右侧桌面壳：外层浅灰蓝画布，应用窗口为近白表面、约 28–32px 大圆角和宽而淡的蓝灰阴影；不照搬左侧营销说明区。
- 应用壳内部比例约为：顶部栏 82px、左侧导航 188–196px、右侧主内容自适应。顶部左侧是真实蓝色品牌标记 + `Stellara Work` + 下拉提示，右侧参考头像必须删除并改为本地工具按钮。
- 侧栏使用“16px 线性图标 + 文字”的纵向导航，活动项为极浅蓝底与品牌蓝文字、12px 左右圆角；导航需映射为首页、项目、任务/会话、文件、设置，不包含团队或账号入口。
- 主内容采用显著留白与中心轴：问候标题、次级说明、约 580×96px 的宽输入器、四个轻量快捷动作；底部三列白色细边框卡片可映射为项目概览、最近会话、工作区状态。
- 色彩应从上一版暖灰改为参考图的冷白/雾蓝体系：画布约 `#F4F7FC`、表面 `#FFFFFF`、主文字近 `#121826`、次文字蓝灰、品牌蓝约 `#2F7CF4`，边框 `#E8EDF5`。阴影存在但非常轻，不使用深色/霓虹/玻璃。
- 项目已有的 `assets/icon-256.png` 与参考图左上品牌图形高度一致（深海军蓝 + 明亮蓝几何标记），应直接作为应用品牌资产；避免重新绘制导致图标比例错误。
- 用户明确排除登录和注册，因此本轮不新增 auth、用户表、登录页、注册页、头像、个人中心或团队成员功能。所有卡片数据来自本地项目、会话和工作目录。
- `ui-ux-pro-max` 的综合检索误判为运营营销页并给出橙色/Lora 方案，与参考图明显冲突，拒绝其具体配色、字体和 landing 结构；参考图像本身是视觉真值。
- 详细风格检索中可采用 Executive Dashboard 的蓝/灰/白专业色、少量摘要卡和一屏总览，以及 150–300ms ease-out、可见焦点、reduced-motion、SVG 图标、明确输入边界等 UX 规则；不采用 Bento 非对称布局、渐变、玻璃或触屏缩放动效。
- 参考图虽然输入框靠 placeholder 表意，实现时仍需保留 `aria-label`/语义标题；桌面按钮视觉尺寸可小于移动端 44px，但命中区和键盘焦点必须清晰。
- `MainView` 的会话、输入、流式执行和文件/设置回调已经集中，适合只增加 `home/work` 视图状态而不改 IPC：首页快捷任务复用同一个输入与发送函数，进入会话时继续沿用现有 ChatStream。
- 当前 Header 有唯一常驻设置按钮而 Sidebar 没有设置入口；参考图对齐后应把“首页/项目/任务/文件/设置”变成侧栏一级导航，并移除 Header 齿轮，避免再次出现两个设置入口。
- Vite 开发预览可在 `http://127.0.0.1:5173/?ui-preview=1` 以本地内存数据运行真实 React 组件；无需启动 Electron 或访问任何账号即可完成参考图对照与响应式检查。
- 1280×720 实景截图显示新版结构与参考图高度一致，且侧栏项目树没有挤压主内容；DOM 中只有本地项目、工作记录、文件、模型和设置，未出现登录、注册、账户、头像或团队成员入口。
- 项目卡采用 CSS Grid 时，DOM 顺序会决定自动放置单元；进度条必须放在箭头之后，否则第三个子项会占用右上单元并把箭头推到下一行。已按图标/正文/箭头/进度条顺序修正。
- 开发预览刷新会按设计回到默认首页，因此刷新后的 DOM 不应被当作“项目页缺失”；页面切换验证需要重新从一级导航进入项目页。
- 设置窗口在新视觉下仍保持 960×656 固定外框，tab 内容只在内部替换；把常驻入口移到侧栏不会影响原设置交互或尺寸稳定策略。
- 900px 宽度下保留 206px 静态侧栏比旧版覆盖侧栏更接近参考图，也不会压坏任务输入器；三张概览卡改为两列，第三张通过主区滚动访问，避免横向滚动。
- 首页式导航与原任务流可以共存：900px 下中栏仍有约 650px，有足够空间保留任务文档、工具结果和固定底部输入器，不需要为参考图外观牺牲现有执行功能。
- 文件浏览窗口视觉已随新 tokens 自动统一，但 DOM 快照没有把其根节点识别为 dialog；需要检查组件并补齐 `role="dialog"`、标题关联和模态语义。
- 文件浏览语义已由组件测试验证，Vite 热更新会关闭当前模态状态但不会产生控制台告警；这属于开发预览状态重置，不影响生产窗口的打开/关闭行为。
- renderer 主 JS 仍约 1.02 MB，延续既有的 Vite 500 kB 大包提示；这是可单独处理的代码分包性能债，不影响本轮 UI 正确性或桌面安装包运行。
- 2026-07-31 用户截图中的原生 Windows 标题栏与最大化状态正常；可见的四边留白和四个圆角来自 `.main-view` 自身的 `width/height: calc(100% - 32px)`、`margin: 16px`、`border-radius: 26px`、边框和阴影。
- 用户所说的“全屏展示”应实现为渲染内容铺满标题栏下方的客户区，而不是启用 F11/kiosk 或移除 Windows 标题栏；这样仍保留熟悉的窗口控制行为。
- `@media (max-width: 700px)` 还会把主壳改为 8px 外边距和 18px 圆角，必须同步删除，否则窄窗口会再次出现四角留白。
- 本轮 `ui-ux-pro-max` 检索返回的营销页红色 CTA 方案不适合现有桌面工作台，继续以 Calm Workspace 为准；只采纳“最外层容器 0px radius”的结构建议，内部组件层级不变。
- 新增的静态样式契约要求 `.main-view` 只存在一个声明块，并固定为 100% 宽高、0 margin、0 border/radius、无阴影；可防止未来响应式规则重新引入四边留白。
- 生产 `dist` 按既有安全边界不包含 `?ui-preview` 内存桥接，普通浏览器直接预览会保持空根节点；真实 DOM 视觉测量必须使用开发模式，而正式 Electron 包仍通过 preload 获取 API。
- 旧 `node_modules/.vite` 缓存由管理员账户创建且当前进程无法清理。自动化视觉检查曾临时把 Vite 缓存指向当前工作区的新目录，完成测量后已删除该缓存并撤回测试性配置；项目默认开发与生产设置不变。
- 真实浏览器 1280×720 测量确认 `.main-view` 为 `x=0, y=0, width=1280, height=720, right=1280, bottom=720`；计算样式是 `margin: 0px`、`border-width: 0px`、`border-radius: 0px`、`box-shadow: none`，document 的 scroll/client 宽高完全相同且控制台无日志。
- 视觉截图确认外层四角已经消失，顶部栏、侧栏和内容面完整贴合客户区边界；首页内部任务输入器与三张信息卡仍保留原圆角，层级没有被扁平化。
- 新安装包已包含边到边布局，大小 117,214,787 字节，SHA-256 为 `1424D2C9188CADA5BD3963FDC7A32954888BED5762F4C9E656C20F10245EEB42`；生产 `dist` 无 `ui-preview` 标记，安装包仍未配置 Authenticode 签名。
- 最终范围核对确认 `vite.config.ts` 中仅有此前已存在的 Electron/Chrome 目标升级差异，本轮临时缓存配置已完全撤回；工作区其他既有改动未被整理、覆盖或回退。
- 2026-07-31 用户截图显示会话操作菜单在选中会话行正下方出现，并被侧栏会话滚动区底边裁去一部分；这表明菜单仍参与局部列表/滚动定位，而不是位于可越过裁切边界的浮层层级。
- 预期交互是菜单贴近会话行的更多按钮，默认向下、底部空间不足时向上展开；打开菜单不应改变列表行位置或侧栏滚动高度。
- `ui-ux-pro-max` 对本缺陷最相关的规则是：overflow menu 应作为独立层级、避免嵌套滚动区裁切和布局位移、危险动作需要文字与危险色、菜单必须键盘可达并能 Escape 退出；无需改变现有 Calm Workspace 配色或字体。
- 本轮设计系统检索再次误判为营销/餐饮落地页，不采用超大字体、Playfair/Karla、绿色 CTA 或 Enterprise Gateway 结构；只保留可见焦点、150–300ms 状态反馈、明确 destructive 色和无结构 emoji 的通用检查项。
- 初步代码定位确认根因：会话菜单 DOM 直接嵌套在 `.session-row` 内，CSS 使用 `position: absolute; top: calc(100% - 2px); right: 4px`，因此必然显示在会话行下方；当会话靠近侧栏列表底部时，又会被上层滚动容器裁切。
- 项目菜单 `.project-action-panel` 是另一套内联面板，本轮只修复 `.session-menu`，避免改变此前已验证的项目操作流程。
- `.session-list` 明确设置了 `overflow: auto`，所以仅提高 `.session-menu` 的 z-index 或改为向上 absolute 都不能从根本上越过裁切边界；稳妥方案是把会话菜单 portal 到 `document.body`，使用 viewport `position: fixed` 与边缘夹紧/向上翻转。
- Sidebar 已有 document click 与 Escape 关闭逻辑，可以复用；新增浮层只需补充坐标状态、菜单尺寸测量、滚动/resize 关闭以及菜单语义，不改 `onRename/onDelete/onExport` 回调。
- 现有 Sidebar 测试使用自建 React root，portal 菜单不在测试 container 内；新增用例应直接从 `document.body` 查询菜单，并断言它不是会话行后代且具有 fixed 坐标。
- Sidebar 返回单个 `<aside>`，会话菜单可以在不改侧栏结构的前提下用 Fragment + `createPortal` 渲染到 body；活动会话可由现有 `menuId/menuType` 与 sessions 派生，无需新增业务状态。
- 右键入口可直接使用 `MouseEvent.clientX/clientY` 作为 viewport 锚点；同时补充 ContextMenu 键和 Shift+F10，从会话行矩形生成键盘锚点，避免新浮层降低键盘可达性。
- 修复后的会话菜单由 `createPortal(..., document.body)` 渲染，使用 fixed left/top；`useLayoutEffect` 在首帧测量真实菜单尺寸并按 8px viewport gutter 夹紧，底部不足时以锚点减去菜单高度实现向上翻转。
- 菜单打开后首项获得焦点且不触发滚动，ArrowUp/ArrowDown/Home/End 可在菜单项间移动；侧栏滚动、窗口 resize、点击外部或 Escape 都会关闭菜单，防止固定浮层与原会话行脱离。
- 本地真实组件预览已正常加载 1280×720 Calm Workspace，侧栏包含 4 条会话；最底部“准备 0.9.0 发布包”适合作为复现截图中底边裁切的验证目标。
- 首轮真实右键验证中菜单能正确 portal 出现，但 document capture scroll 监听会被浏览器为焦点/可见性产生的滚动事件触发，导致菜单在下一次检查前关闭；viewport 锚定菜单不需要跟随行滚动，因此移除该过度关闭条件，保留 resize、外部点击与 Escape 关闭。
- 为直接解决“显示在下方”，翻转边界不仅取 viewport 底部，还取 `.session-list` 可见底边：菜单若会越过会话列表底部，即使页面下方仍有空间，也优先在当前会话上方展开；portal 继续保证不会被 overflow 裁切。
- 第二轮真实测量确认菜单为 body 直属 fixed 浮层，160×78px，底部会话打开时从行下方翻到 `top=606.05, bottom=684.05`，完整位于 `row.top=688.05` 之上，且菜单与 viewport/会话列表底边均保留安全空间。
- 实景截图同时显示按鼠标 x 锚定会让 160px 菜单部分伸入主内容区；最终再用 session-list 右边界夹紧，使菜单保留在侧栏内，减少对工作记录的遮挡。
- 最终真实测量通过：菜单 `left=47, right=207`，会话列表右边界为 215（保留 8px）；菜单 `top=606.05, bottom=684.05`，目标行 `top=688.05`，完整位于行上方。菜单仍是 body 直属 fixed 元素，列表 `scrollHeight === clientHeight === 262`，没有挤压或新增滚动高度。
- 最终截图确认“重命名 / 删除”浮层完整显示在侧栏范围内，删除为明确危险色，未再出现截图中的下方残缺菜单，也没有遮挡主工作区。
- 新安装包已包含会话菜单修复，大小 117,215,090 字节，SHA-256 为 `AF2C42F376035A6BAD164FCF3A0D2CC1AF324C348E80A465636E95E100DEBEF7`；生产 `dist` 无 `ui-preview` 标记，安装包仍未配置 Authenticode 签名。
- 2026-07-31 用户最新截图中的黑色横条属于 Windows 原生 BrowserWindow 标题栏，不是 renderer 内的 Header；左侧小图标与 `Stellara Work` 文字也由原生窗口框架绘制。
- 用户要求的“沉浸式”应实现为 hidden title bar + native title bar overlay：renderer 延伸到窗口顶部，不再显示系统应用图标和标题文字，同时继续保留原生最小化、最大化、关闭按钮与系统窗口行为。
- 顶部 Header 需要设置可拖拽区域，而所有按钮、选择器、链接和其他交互控件必须显式设为 no-drag；右上角还需预留 Windows caption buttons 的安全宽度，避免现有工具按钮重叠。
- 本轮继续采用 Calm Workspace 视觉规范；`ui-ux-pro-max` 的关键约束是优先系统控件、确保系统栏安全区、保持键盘焦点与可点击命中区，不需要改变现有配色、字体或信息架构。
- 本轮强制设计系统检索再次误判为 App Store 落地页与 VisionOS 空间玻璃风，和用户明确的非科幻桌面工作台方向冲突；不采用其玻璃、景深、橙绿配色和在线字体，仅保留系统控件优先、可见焦点、150–300ms 反馈与 reduced-motion 检查项。
- 本地 Electron 43.2.0 类型定义确认 `BrowserWindow` 支持 `titleBarStyle: 'hidden'`、`titleBarOverlay` 的 CSS color/symbolColor/height，并会暴露 Window Controls Overlay 的 CSS 环境变量；无需新增 IPC 或自绘窗口按钮。
- 现有 `.main-header` 高度正好为 72px，且已经设置 `-webkit-app-region: drag`、button/model/menu 的 no-drag；实现只需把 overlay 高度对齐 72px，并通过 `titlebar-area-x/width` 动态计算右上 caption buttons 安全宽度。
- 工作区包含大量此前未提交改动，`electron/main.ts` 也承载 Agent/IPC 增量；本轮只在既有 BrowserWindow options 中追加标题栏配置，不整理或覆盖其他差异。
- Electron 官方文档确认：Windows 上 `titleBarStyle: hidden` 与 `titleBarOverlay` 组合会隐藏默认标题栏并保留原生窗口控制；官方兼容建议正是使用 `env(titlebar-area-x, 0px)` 与 `env(titlebar-area-width, 100%)` 约束安全区域，本轮实现与 Electron 43.2.0 本地类型一致。
- 完整 renderer 回归已通过：13 个 UI/样式测试文件共 112 项成功，TypeScript 检查和 `git diff --check` 均无错误；renderer 生产构建成功，CSS 65.48 kB（gzip 11.53 kB）。
- Vite 继续报告既有主 JS 约 1.02 MB、超过 500 kB 的代码分包提示；本轮没有扩大业务入口或新增运行时依赖，该性能债不阻塞沉浸式窗口交付。
- 原生 caption symbol 最终采用 `#65758B`：对白色 Header 对比度约 4.70:1，对深色 `#1D293D` Header 约 3.11:1，比初版蓝灰在深色主题上更清楚，同时不会在浅色主题显得突兀。
- 最终 Electron/NSIS 打包成功；编译后的 `electron/dist/electron/main.js` 明确包含 hidden title bar、透明 overlay、`#65758B` symbol 与 72px 高度，生产 CSS 明确包含 `titlebar-area-x/width` 安全区计算。
- 最终安装包 `release/Stellara Work-Setup-0.9.0.exe` 为 117,215,132 字节，SHA-256 `91FEF965BECED4199E331B3CBC9D3CEAB58BBE5859C0ABB6CAD70AC7BA601E49`，Authenticode 状态为 `NotSigned`。
- 额外尝试通过 `@electron/asar.extractFile()` 读取打包归档内部 main.js 时，Windows 风格归档路径在该 API 中持续不被接受；这项非必要深检已停止，electron-builder 成功输出、编译产物内容与安装包哈希已完成交付所需验证。
- 2026-08-01 用户反馈顶栏“工作区”按钮存在间歇性失效：点击后右侧 WorkspacePanel 有时没有展开；本轮先核对事件、状态和条件渲染链路，不假定是纯 CSS 或动画问题。
- 本轮 UI 规范重点是状态清晰、即时反馈、稳定布局和可访问状态同步；修复后按钮 `aria-pressed` 与右侧面板是否真实存在必须保持同一真值，不通过装饰动画或延时重试掩盖失败。
- 强制设计系统检索再次误判为 App Store 落地页并推荐青橙配色与在线字体，不采用这些视觉建议；仅保留其“微交互必须即时、状态变化要清楚、避免慢反馈”的通用原则。
- 根因已定位：Header 的工作区按钮在所有 `activeSection` 中始终渲染，App 也会正常切换 `workspaceOpen`；但 MainView 第 517 行只有 `activeSection === 'tasks' && props.workspaceOpen && config.workDir` 时才创建 WorkspacePanel。
- 因此在首页或项目页点击时，`workspaceOpen` 和按钮 `aria-pressed` 会变成 true，右侧面板却被 `activeSection` 条件拦截；切到工作记录页后才突然出现，形成用户观察到的“有时候不展开”。这不是点击丢失、文件加载失败或 CSS 动画问题。
- 工作区按钮和快捷键本身是全局 UI，WorkspacePanel 的目标/进度/文件也不依赖工作记录页 DOM；最小且一致的修复是去掉 `activeSection === 'tasks'` 限制，仅以 `workspaceOpen + workDir` 作为单一可见性真值。
- WorkspacePanel 组件本身只依赖 `workDir`、目标、进度、交付物和文件状态，没有读取 `activeSection`，所以跨首页/项目页渲染不会改变其文件读取或任务语义。
- 现有测试只覆盖 WorkspacePanel 自身和 Header 基础结构，没有保护“Header 全局可见、面板却仅 tasks 渲染”的跨组件契约；本轮增加源契约断言，并为按钮补 `aria-controls/aria-expanded`、为面板补稳定 id。
- 修复后的定向回归通过：Header、WorkspacePanel、样式/跨层契约共 3 个测试文件、50 项成功；renderer 与 Electron TypeScript 检查均通过。
- 本轮使用应用内浏览器做本地真实 React 交互验证，因为缺陷与当前页面状态有关，单独组件测试无法证明首页/项目页点击后面板实际出现在 DOM 和布局中。
- 1280×720 首页真实点击已通过：`#workspace-panel` 可见尺寸为 280×648，按钮 `aria-pressed` 与 `aria-expanded` 同步为 `true`，document `scrollWidth === clientWidth === 1280`，没有横向溢出。
- 旧版必现失败的项目页也已通过真实点击：主标题保持“项目”，右侧面板立即以 280×648 显示，按钮状态同步为展开，页面宽度仍为 1280/1280、无横向溢出。
- 项目页展开后的真实截图确认面板与项目卡片边界清楚、顶栏按钮保持选中态；浏览器控制台 `warn/error` 均为空。验证所用临时 Vite 缓存配置、缓存目录和预览进程已全部撤回。
- 修复版 NSIS 安装包已生成：117,215,295 字节，SHA-256 `1BAC6816DECB226F855EBA6167A8A916B76960E42BBE7589CF0C821AF6166298`；Authenticode 为 `NotSigned`，与项目当前未配置证书的状态一致。
- 2026-08-01 新任务：为左侧具体项目项增加项目窗口，窗口顶部编辑项目名称，下方选择已有文件或新建文件；一级导航“项目”仍应进入项目总览，避免同名入口语义冲突。
- 本轮强制设计系统检索误判成 Portfolio Grid，并推荐琥珀 CTA 与在线 Lora/Raleway；这些与既有 Calm Workspace 和离线 Windows 桌面产品冲突，不采用。保留其 150–200ms 反馈、可见焦点、无 emoji、清楚错误与 reduced-motion 原则。
- 初步代码核对：左侧具体项目由 `Sidebar.renderProjectGroup()` 渲染，当前整行点击只执行 `toggleProject(p.id)`，项目名称双击进入行内重命名，更多菜单另有重命名/新建会话/删除；新增窗口必须把“打开项目窗口”和“展开会话”拆成独立命中区，不能丢失现有折叠能力。
- `Project` 数据包含可选 `workDir`，但当前 `ProjectSummary` 没有带出该字段，侧栏拿不到项目目录；renderer 也只有目录树、读取文件与打开路径，没有“选择单个文件”或“安全新建空文件”的专用 IPC。需要先把项目目录送到摘要，再补窄权限 IPC，不能让 renderer 直接访问 Node 文件系统。
- 现有文件安全层已经覆盖工作区允许列表、路径越界和 symlink 真实路径检查；新建文件应复用这里的写入路径校验，使用独占创建避免静默覆盖已有文件。选择文件由主进程原生对话框完成，并再次验证选中结果仍在项目工作目录内。
- 现有 `FileTreeModal` 提供完整目录树预览，但用户要的是项目项点击后的“小窗口”；本轮不会把大号文件浏览器直接塞进侧栏，而采用顶部名称字段 + 下方两个并列文件动作 + 最近选择/创建结果的紧凑结构。
- 现有样式系统已经有统一 modal/backdrop、按钮、输入框与 150–240ms 入场动效，图标集也具备 folder/file/plus/edit/x；项目窗口可完全复用，无需引入新依赖、emoji 或新色板。
- 现有 Sidebar 测试把更多菜单“重命名”绑定到行内输入框。窗口上线后会把该动作改为打开同一个项目窗口，并新增独立组件测试覆盖名称保存、文件选择、新建、Escape 与错误反馈。
- `projects:list` 当前主动丢弃数据库 Project 的 `workDir`，`MainView` 新建项目也没有传当前 `config.workDir`，App 接收新项目后再次构造摘要时同样丢弃目录；这三处必须一起修正，否则窗口只能依赖临时全局回退。
- 项目窗口放在 Sidebar 内管理最贴近触发点：Sidebar 接收 `defaultWorkDir`，按 `project.workDir ?? defaultWorkDir` 计算文件范围；具体项目行主体打开 dialog，箭头按钮继续展开/收起，一级导航“项目”保持进入项目总览。
- 首轮项目窗口 CSS 使用了四个未定义的猜测令牌；静态检索立即发现后已改用现有 Calm Workspace 的 `accent/accent-tint/success-bg/danger-bg` 令牌，避免浅深主题出现无效样式。
- 新文件 helper 使用相对路径、现有 `verifyWritePath`、已存在父目录检查和 `fs.open(..., 'wx')` 独占创建：越界、缺失父目录和同名覆盖都会被明确拒绝；新增 4 项测试已验证这些边界。
- 项目窗口桌面宽度固定为最多 540px、内容区独立滚动；700px 以下名称行、文件动作和新建表单自动改单列，选中文件操作换到第二行，避免窄窗口横向溢出。
- 本地真实界面已验证具体项目名称按钮打开 `项目设置` dialog，而左侧 chevron 保持独立；选择文件后只显示路径与显式“打开文件”，新建 `docs/demo.md` 后在窗口内反馈成功，不会自动跳走。
- 项目名称保存会同步更新侧栏项目名与首页项目卡片，弹窗保持打开并给出成功状态；900×700 窄桌面视口下 540px 弹窗完整可见、无横向溢出，文件动作与结果区保持清楚层级。
- 首次在同一浏览器自动化指令内连续执行“完成 + 收起”时，第二次点击与 React 卸载时序重叠，快照仍显示展开；拆成独立操作后立即正确收起，确认不是产品状态缺陷，并补充独立 chevron 回归测试保护该行为。
- 收尾 UX 检索只命中 reduced-motion、焦点可见性与避免强制滚动；项目已通过全局 `prefers-reduced-motion`、输入框焦点环、按钮 focus-visible 和不使用滚动劫持满足这些高优先级规则。
- 完整源回归为 18 个测试文件、147 项成功；renderer 生产构建 CSS 72.97 kB（gzip 12.52 kB），主 JS 仍约 1.03 MB 并保留既有 >500 kB 分包提示，本轮功能未引入新运行时依赖。
- 新安装包编译后主进程明确包含 `dialog:openFile` 与 `fs:createFile`，renderer bundle 包含“项目设置”；最终 NSIS 文件为 117,218,511 字节，SHA-256 `D5ABA59D9DAFCE2B3E37F45BD97DD23B2B1128824C2BB90A95388950F38B9BB3`，当前仍未配置 Authenticode 证书。
- 2026-08-01 用户澄清真实意图：项目小窗口属于“创建前设置”，而不是创建完成后点击项目主体再打开；现有侧栏按钮和项目页按钮都直接调用 `projects.create({ name: '新项目' })`，因此必须把写库动作推迟到弹窗最终提交。
- 已有项目主体应恢复原来的展开/收起语义；为避免丢失已经实现的重命名与文件管理，更多菜单中的“编辑项目”可以继续打开同一组件的编辑模式，但普通点击不再进入设置。
- 创建模式最稳妥的结构是：顶部必填项目名称、下方可选文件操作、底部“取消 / 创建项目”。取消只关闭窗口且不调用 `projects.create`；文件操作是用户显式行为，仍使用当前 workDir 和既有安全 IPC。
- 本轮设计系统检索再次误判为 Portfolio Grid、橙色活动页和 Lora/Raleway；不采纳视觉结果，继续以已持久化的 Calm Workspace 为准，只保留单一主 CTA、可见焦点、150–200ms 反馈、清楚错误和 reduced-motion 原则。
- `ProjectDialog` 现有名称、文件选择和新建文件逻辑可安全复用；新增 create mode 后名称初始为空、文件为可选、底部提供唯一主动作“创建项目”，只有该提交会调用 MainView 的 `projects.create`。
- 侧栏已恢复为单个项目 toggle 按钮（箭头、名称、计数同一命中区），普通点击不会出现 dialog；更多菜单“编辑项目”仍保留，避免重命名能力回退。
- 所有创建入口现在集中为 `setCreateProjectOpen(true)`；MainView 的 `handleCreateProject` 是唯一写库路径，成功后更新 App 状态并关闭窗口，失败由弹窗保留名称并就地显示错误。
- 本地真实预览确认侧栏“新建项目”点击后项目数仍为 2，随后出现可访问名称为“创建项目”的 dialog；名称字段为空且自动聚焦，最终创建按钮禁用，说明没有提前生成“新项目”记录。
- 创建前窗口实景保持原 Calm Workspace 的 540px 紧凑尺寸，名称、可选文件动作与底部主次按钮层级清楚；点击取消后 dialog 消失，侧栏仍只有原 2 个项目，没有占位项目。
- 正式创建路径实测通过：输入“界面验证项目”并选择 README.md 时项目数仍为 2；点击底部“创建项目”后 dialog 才关闭，新项目以 0 条记录出现在侧栏和首页，项目总数精确变为 3。
- 已有“桌面端产品”普通点击实测只把 `aria-expanded` 从 true 改为 false 并隐藏两条会话，未打开 dialog；切换到项目总览后，页面顶部“新建项目”也打开同一个创建前窗口，两个入口行为一致。
- 创建、取消、文件选择、现有项目折叠和项目页入口的真实操作均未产生 renderer warn/error；验证标签页、Vite 进程、隔离缓存和临时配置已全部清理。
- 交付前 UX 检索补充了一项有效要求：必填字段不能让用户猜测。创建模式的“项目名称”现同时显示“必填”文字并设置原生 `required`，编辑模式不显示该标记；全局 reduced-motion 和可见焦点规则继续生效。
- 最终源码回归为 18 个测试文件、152 项全部通过；renderer/Electron TypeScript、`git diff --check` 与生产构建成功。CSS 73.00 kB（gzip 12.54 kB），主 JS 约 1.03 MB，继续保留既有 >500 kB 分包提示。
- 新安装包的 renderer bundle 已确认包含“项目将在完成设置后创建”“关闭窗口不会生成占位项目”和“必填”文案；NSIS 文件为 117,218,894 字节，SHA-256 `CEBFDB6B603E073570726A1D51BCA2CAA74166D2FA4438851EC8C4102555FCBB`，Authenticode 仍为 `NotSigned`。
