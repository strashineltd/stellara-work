# Stellara Work 0.9.3 界面外壳重构实施计划（Plan 1/3）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按参考图 1 重写应用外壳（顶栏、侧栏 IA、首页、会话视图头部、设置皮肤、PR/已安排占位页），本地模式全部功能保持可用。

**Architecture:** 渲染层新增 shell/home 组件与导航历史;MainView 从"Header + HomeDashboard"切换为"AppTopBar + HomeView";执行端选择器预留插槽（Plan 2 注入服务器选择器）;服务器会话数据在 Plan 2/3 接入，本计划不碰主进程 Agent 行为（仅新增 git 分支查询 IPC）。

**Tech Stack:** React 19 + TypeScript + Vite + Vitest（jsdom）+ Electron 主进程 IPC。

**Spec:** `docs/superpowers/specs/2026-09-10-v0.9.3-redesign-server-scheduler-design.md`

## Global Constraints

- 保持无账号登录 UI：不出现登录/注册/账户/头像/Plus 文案。
- 本地模式行为不回归：对话、审批、计划模式、附件、记忆、文件、设置全部可用。
- 本计划不新增任何 npm 依赖。
- 无边框窗口与 macOS 红绿灯布局约束保持；顶栏作为拖拽区，交互控件 `-webkit-app-region: no-drag`。
- 中文文案与现有组件命名风格一致；类型严格（`npm run typecheck` 通过）。
- 每个任务结束必须跑 `npm test`；最终跑 `npm run typecheck && npm test && npm run build`。
- 旧组件 `Header` / `HomeDashboard` 在 Plan 1 内删除（任务 9），避免双份维护。

---

### Task 1: 导航模型与历史栈

**Files:**
- Create: `src/lib/navigation.ts`
- Create: `src/hooks/useNavHistory.ts`
- Test: `src/hooks/useNavHistory.test.tsx`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `AppSection = 'home' | 'tasks' | 'memory' | 'files' | 'pull-requests' | 'scheduled'`
  - `NavState = { section: AppSection; sessionId: string | null }`
  - `useNavHistory(initial: NavState): { current: NavState; canGoBack: boolean; canGoForward: boolean; push(next: NavState): void; replace(next: NavState): void; back(): void; forward(): void }`
  - `ApprovalMode = 'auto' | 'step' | 'plan'`（同文件导出）

- [ ] **Step 1: Write the failing test**

```tsx
// src/hooks/useNavHistory.test.tsx
import { act } from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { beforeEach, describe, expect, it } from 'vitest';
import { useNavHistory, type NavState } from './useNavHistory';
import { INITIAL_NAV } from '../lib/navigation';

let api: ReturnType<typeof useNavHistory> | null = null;

function Harness() {
  api = useNavHistory(INITIAL_NAV);
  return <span>{`${api.current.section}:${api.current.sessionId ?? '-'}:${String(api.canGoBack)}:${String(api.canGoForward)}`}</span>;
}

function renderHarness() {
  const container = document.createElement('div');
  document.body.appendChild(container);
  const root: Root = createRoot(container);
  act(() => { root.render(<Harness />); });
  return { container, root, unmount: () => { act(() => root.unmount()); container.remove(); } };
}

describe('useNavHistory', () => {
  beforeEach(() => { api = null; document.body.innerHTML = ''; });

  it('starts at the initial state with no history', () => {
    const view = renderHarness();
    expect(api!.current).toEqual(INITIAL_NAV);
    expect(api!.canGoBack).toBe(false);
    expect(api!.canGoForward).toBe(false);
    view.unmount();
  });

  it('pushes, goes back and forward', () => {
    const view = renderHarness();
    const session: NavState = { section: 'tasks', sessionId: 's1' };
    act(() => { api!.push(session); });
    expect(api!.current).toEqual(session);
    expect(api!.canGoBack).toBe(true);
    act(() => { api!.back(); });
    expect(api!.current).toEqual(INITIAL_NAV);
    expect(api!.canGoForward).toBe(true);
    act(() => { api!.forward(); });
    expect(api!.current).toEqual(session);
    view.unmount();
  });

  it('push clears forward history and replace does not grow history', () => {
    const view = renderHarness();
    act(() => { api!.push({ section: 'memory', sessionId: null }); });
    act(() => { api!.back(); });
    act(() => { api!.push({ section: 'files', sessionId: null }); });
    expect(api!.canGoForward).toBe(false);
    act(() => { api!.replace({ section: 'scheduled', sessionId: null }); });
    expect(api!.current.section).toBe('scheduled');
    act(() => { api!.back(); });
    expect(api!.current.section).toBe('home');
    view.unmount();
  });

  it('pushing the same state is a no-op', () => {
    const view = renderHarness();
    act(() => { api!.push(INITIAL_NAV); });
    expect(api!.canGoBack).toBe(false);
    view.unmount();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/hooks/useNavHistory.test.tsx`
Expected: FAIL — `Cannot find module './useNavHistory'`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/navigation.ts
export type AppSection = 'home' | 'tasks' | 'memory' | 'files' | 'pull-requests' | 'scheduled';

export interface NavState {
  section: AppSection;
  sessionId: string | null;
}

export const INITIAL_NAV: NavState = { section: 'home', sessionId: null };

export type ApprovalMode = 'auto' | 'step' | 'plan';
```

```ts
// src/hooks/useNavHistory.ts
import { useCallback, useRef, useState } from 'react';
import type { NavState } from '../lib/navigation';

function equalNav(a: NavState, b: NavState): boolean {
  return a.section === b.section && a.sessionId === b.sessionId;
}

export function useNavHistory(initial: NavState) {
  const [current, setCurrent] = useState<NavState>(initial);
  const past = useRef<NavState[]>([]);
  const future = useRef<NavState[]>([]);
  const [, forceRender] = useState(0);

  const sync = useCallback(() => { forceRender((n) => n + 1); }, []);

  const push = useCallback((next: NavState) => {
    setCurrent((prev) => {
      if (equalNav(prev, next)) return prev;
      past.current.push(prev);
      future.current = [];
      return next;
    });
    sync();
  }, [sync]);

  const replace = useCallback((next: NavState) => {
    setCurrent(next);
  }, []);

  const back = useCallback(() => {
    const prev = past.current.pop();
    if (!prev) return;
    setCurrent((cur) => {
      future.current.push(cur);
      return prev;
    });
    sync();
  }, [sync]);

  const forward = useCallback(() => {
    const next = future.current.pop();
    if (!next) return;
    setCurrent((cur) => {
      past.current.push(cur);
      return next;
    });
    sync();
  }, [sync]);

  return {
    current,
    canGoBack: past.current.length > 0,
    canGoForward: future.current.length > 0,
    push,
    replace,
    back,
    forward,
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/hooks/useNavHistory.test.tsx`
Expected: PASS（4 个用例）。

- [ ] **Step 5: Typecheck and commit**

Run: `npm run typecheck`
Expected: 通过。

```bash
git add src/lib/navigation.ts src/hooks/useNavHistory.ts src/hooks/useNavHistory.test.tsx
git commit -m "feat(shell): 导航模型与历史栈 hook"
```

---

### Task 2: 图标扩展与顶栏组件

**Files:**
- Modify: `src/components/Icon.tsx`（IconName 联合 + switch 分支）
- Create: `src/components/shell/AppTopBar.tsx`
- Test: `src/components/shell/AppTopBar.test.tsx`

**Interfaces:**
- Consumes: `Icon`（现有）。
- Produces: `<AppTopBar canGoBack canGoForward onBack onForward sidebarOpen workspaceOpen onToggleSidebar onToggleWorkspace executionTarget? />`；根元素类名 `app-topbar`。

- [ ] **Step 1: Write the failing test**

```tsx
// src/components/shell/AppTopBar.test.tsx
import { act } from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { AppTopBar } from './AppTopBar';

const BASE_PROPS = {
  canGoBack: false,
  canGoForward: false,
  onBack: vi.fn(),
  onForward: vi.fn(),
  sidebarOpen: true,
  workspaceOpen: false,
  onToggleSidebar: vi.fn(),
  onToggleWorkspace: vi.fn(),
};

function render(ui: React.ReactElement) {
  const container = document.createElement('div');
  document.body.appendChild(container);
  const root: Root = createRoot(container);
  act(() => { root.render(ui); });
  return {
    container,
    unmount: () => { act(() => root.unmount()); container.remove(); },
  };
}

function fireClick(element: Element | null) {
  if (!element) throw new Error('Element not found for click');
  act(() => element.dispatchEvent(new MouseEvent('click', { bubbles: true })));
}

describe('AppTopBar', () => {
  beforeEach(() => { document.body.innerHTML = ''; vi.clearAllMocks(); });

  it('disables back/forward when history is empty', () => {
    const { container, unmount } = render(<AppTopBar {...BASE_PROPS} />);
    expect(container.querySelector<HTMLButtonElement>('[aria-label="后退"]')?.disabled).toBe(true);
    expect(container.querySelector<HTMLButtonElement>('[aria-label="前进"]')?.disabled).toBe(true);
    unmount();
  });

  it('calls back and forward when enabled', () => {
    const onBack = vi.fn();
    const onForward = vi.fn();
    const { container, unmount } = render(
      <AppTopBar {...BASE_PROPS} canGoBack canGoForward onBack={onBack} onForward={onForward} />,
    );
    fireClick(container.querySelector('[aria-label="后退"]'));
    fireClick(container.querySelector('[aria-label="前进"]'));
    expect(onBack).toHaveBeenCalledTimes(1);
    expect(onForward).toHaveBeenCalledTimes(1);
    unmount();
  });

  it('renders the local execution target by default and a custom slot when provided', () => {
    const first = render(<AppTopBar {...BASE_PROPS} />);
    expect(first.container.querySelector('.execution-target-chip')?.textContent).toContain('本地');
    first.unmount();

    const second = render(<AppTopBar {...BASE_PROPS} executionTarget={<button type="button">自定义服务器</button>} />);
    expect(second.container.textContent).toContain('自定义服务器');
    expect(second.container.querySelector('.execution-target-chip')).toBeNull();
    second.unmount();
  });

  it('exposes panel toggles with pressed state', () => {
    const onToggleSidebar = vi.fn();
    const onToggleWorkspace = vi.fn();
    const { container, unmount } = render(
      <AppTopBar {...BASE_PROPS} sidebarOpen={false} workspaceOpen onToggleSidebar={onToggleSidebar} onToggleWorkspace={onToggleWorkspace} />,
    );
    const sidebar = container.querySelector<HTMLButtonElement>('[aria-label="显示侧栏"]');
    expect(sidebar?.getAttribute('aria-pressed')).toBe('false');
    fireClick(sidebar);
    expect(onToggleSidebar).toHaveBeenCalledTimes(1);
    const workspace = container.querySelector<HTMLButtonElement>('[aria-label="隐藏工作区"]');
    expect(workspace?.getAttribute('aria-pressed')).toBe('true');
    fireClick(workspace);
    expect(onToggleWorkspace).toHaveBeenCalledTimes(1);
    unmount();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/shell/AppTopBar.test.tsx`
Expected: FAIL — `Cannot find module './AppTopBar'`。

- [ ] **Step 3: Implement icons and component**

在 `src/components/Icon.tsx` 的 `IconName` 联合中加入：

```ts
  | 'chevron-left'
  | 'bell'
  | 'calendar'
  | 'server'
```

在 switch 中加入分支：

```tsx
    case 'chevron-left':
      return <svg {...common}><path d="M10 4 6 8l4 4" /></svg>;
    case 'bell':
      return <svg {...common}><path d="M8 2.5a3.5 3.5 0 0 0-3.5 3.5v2.2L3.2 10.5h9.6L11.5 8.2V6A3.5 3.5 0 0 0 8 2.5Z" /><path d="M6.8 12.5a1.3 1.3 0 0 0 2.4 0" /></svg>;
    case 'calendar':
      return <svg {...common}><rect x="2.5" y="3.5" width="11" height="10" rx="1.5" /><path d="M2.5 6.5h11M5.5 2.5v2M10.5 2.5v2" /></svg>;
    case 'server':
      return <svg {...common}><rect x="2.5" y="3" width="11" height="4" rx="1" /><rect x="2.5" y="9" width="11" height="4" rx="1" /><path d="M5 5h.01M5 11h.01" /></svg>;
```

```tsx
// src/components/shell/AppTopBar.tsx
import type { ReactNode } from 'react';
import { Icon } from '../Icon';

interface AppTopBarProps {
  canGoBack: boolean;
  canGoForward: boolean;
  onBack: () => void;
  onForward: () => void;
  sidebarOpen: boolean;
  workspaceOpen: boolean;
  onToggleSidebar: () => void;
  onToggleWorkspace: () => void;
  /** Plan 2 注入服务器选择器；缺省渲染静态「本地」chip */
  executionTarget?: ReactNode;
}

export function AppTopBar(props: AppTopBarProps) {
  return (
    <header className="app-topbar">
      <div className="app-topbar__nav">
        <button className="btn-icon app-topbar__nav-button" type="button" aria-label="后退" disabled={!props.canGoBack} onClick={props.onBack}>
          <Icon name="chevron-left" size={15} />
        </button>
        <button className="btn-icon app-topbar__nav-button" type="button" aria-label="前进" disabled={!props.canGoForward} onClick={props.onForward}>
          <Icon name="chevron-right" size={15} />
        </button>
      </div>
      <div className="app-topbar__center" />
      <div className="app-topbar__actions">
        {props.executionTarget ?? (
          <span className="execution-target-chip">
            <i aria-hidden="true" />
            本地
          </span>
        )}
        <button
          className="btn-icon app-topbar__action-button"
          type="button"
          aria-label={props.sidebarOpen ? '隐藏侧栏' : '显示侧栏'}
          aria-pressed={props.sidebarOpen}
          onClick={props.onToggleSidebar}
        >
          <Icon name="panel-left" size={15} />
        </button>
        <button
          className="btn-icon app-topbar__action-button"
          type="button"
          aria-label={props.workspaceOpen ? '隐藏工作区' : '显示工作区'}
          aria-pressed={props.workspaceOpen}
          onClick={props.onToggleWorkspace}
        >
          <Icon name="panel-right" size={15} />
        </button>
      </div>
    </header>
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/components/shell/AppTopBar.test.tsx && npm run typecheck`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/Icon.tsx src/components/shell/AppTopBar.tsx src/components/shell/AppTopBar.test.tsx
git commit -m "feat(shell): 顶栏组件与图标扩展"
```

---

### Task 3: Git 分支查询 IPC

**Files:**
- Create: `electron/git-branch.ts`
- Test: `electron/git-branch.test.ts`
- Modify: `electron/main.ts`（注册 handler）、`electron/preload.ts`、`shared/ipc.ts`（ElectronAPI.app）、`src/dev-preview.ts`（stub）

**Interfaces:**
- Consumes: 无。
- Produces:
  - `readGitBranch(workDir: string): Promise<string | null>`
  - IPC `app:getGitBranch`，renderer 侧 `window.electronAPI.app.getGitBranch(workDir: string): Promise<string | null>`

- [ ] **Step 1: Write the failing test**

```ts
// electron/git-branch.test.ts
import { mkdtemp, mkdir, writeFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import path from 'node:path';
import { afterEach, describe, expect, it } from 'vitest';
import { readGitBranch } from './git-branch';

const dirs: string[] = [];

async function fixture(): Promise<string> {
  const dir = await mkdtemp(path.join(tmpdir(), 'stellara-git-'));
  dirs.push(dir);
  return dir;
}

afterEach(async () => {
  await Promise.all(dirs.splice(0).map((dir) => rm(dir, { recursive: true, force: true })));
});

describe('readGitBranch', () => {
  it('reads a symbolic ref from .git/HEAD', async () => {
    const dir = await fixture();
    await mkdir(path.join(dir, '.git'));
    await writeFile(path.join(dir, '.git', 'HEAD'), 'ref: refs/heads/feature/shell\n');
    expect(await readGitBranch(dir)).toBe('feature/shell');
  });

  it('walks up to find .git/HEAD', async () => {
    const dir = await fixture();
    await mkdir(path.join(dir, '.git'));
    await writeFile(path.join(dir, '.git', 'HEAD'), 'ref: refs/heads/main\n');
    const nested = path.join(dir, 'src', 'deep');
    await mkdir(nested, { recursive: true });
    expect(await readGitBranch(nested)).toBe('main');
  });

  it('returns a short hash for detached HEAD', async () => {
    const dir = await fixture();
    await mkdir(path.join(dir, '.git'));
    await writeFile(path.join(dir, '.git', 'HEAD'), '9c4f1a2b3d4e5f60718293a4b5c6d7e8f9012345\n');
    expect(await readGitBranch(dir)).toBe('9c4f1a2');
  });

  it('returns null outside a repository and for missing paths', async () => {
    const dir = await fixture();
    expect(await readGitBranch(dir)).toBeNull();
    expect(await readGitBranch(path.join(dir, 'missing'))).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run electron/git-branch.test.ts`
Expected: FAIL — `Cannot find module './git-branch'`。

- [ ] **Step 3: Implement main-process function**

```ts
// electron/git-branch.ts
import { promises as fs } from 'node:fs';
import path from 'node:path';

const MAX_LEVELS = 10;

async function readHead(gitDir: string): Promise<string | null> {
  try {
    return await fs.readFile(path.join(gitDir, 'HEAD'), 'utf-8');
  } catch {
    return null;
  }
}

/** 读取 workDir 所在仓库的当前分支；detached HEAD 返回 7 位短 hash；非 git 目录返回 null。 */
export async function readGitBranch(workDir: string): Promise<string | null> {
  let current = path.resolve(workDir);
  for (let i = 0; i < MAX_LEVELS; i++) {
    const dotGit = path.join(current, '.git');
    const head = await readHead(dotGit);
    if (head !== null) {
      const text = head.trim();
      if (text.startsWith('ref: refs/heads/')) return text.slice('ref: refs/heads/'.length);
      if (/^[0-9a-f]{40}$/i.test(text)) return text.slice(0, 7);
      return null;
    }
    const parent = path.dirname(current);
    if (parent === current) break;
    current = parent;
  }
  return null;
}
```

在 `electron/main.ts` 的 app 相关 handler 附近注册（`app:getInfo` 之后）：

```ts
  handle('app:getGitBranch', async (_e, workDir: string) => {
    const { readGitBranch } = await import('./git-branch');
    if (typeof workDir !== 'string' || !workDir.trim()) return null;
    return readGitBranch(workDir);
  });
```

在 `shared/ipc.ts` 的 `ElectronAPI.app` 中加：

```ts
    getGitBranch: (workDir: string) => Promise<string | null>;
```

在 `electron/preload.ts` 的 `app` 命名空间中加：

```ts
    getGitBranch: (workDir: string): Promise<string | null> => ipcRenderer.invoke('app:getGitBranch', workDir),
```

在 `src/dev-preview.ts` 的 electronAPI stub 的 `app` 部分加：

```ts
    getGitBranch: async () => 'main',
```

- [ ] **Step 4: Run tests and typecheck**

Run: `npx vitest run electron/git-branch.test.ts && npm run typecheck`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add electron/git-branch.ts electron/git-branch.test.ts electron/main.ts electron/preload.ts shared/ipc.ts src/dev-preview.ts
git commit -m "feat(app): Git 分支查询 IPC"
```

---

### Task 4: 从 Header 抽取 ModelSwitcher

**Files:**
- Create: `src/components/chat/ModelSwitcher.tsx`
- Test: `src/components/chat/ModelSwitcher.test.tsx`
- Modify: `src/components/chat/Header.tsx`（内部改用 ModelSwitcher，DOM 结构保持不变）

**Interfaces:**
- Consumes: `ConfiguredModel`、`ModelListItem`（`shared/ipc`）、`usePresence`、`Icon`。
- Produces: `<ModelSwitcher config modelList switchingModel onSwitchModel onReconfigure />`；菜单类名保持 `model-switcher` / `model-menu` 等现有类，确保 `Header.test.tsx` 无需改动。

- [ ] **Step 1: Write the failing test**

```tsx
// src/components/chat/ModelSwitcher.test.tsx
import { act } from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { beforeEach, describe, expect, it, vi } from 'vitest';
import type { ConfiguredModel, ModelListItem } from '../../../shared/ipc';
import { ModelSwitcher } from './ModelSwitcher';

const CONFIG: ConfiguredModel = {
  id: 'm1', label: '测试模型', model: 'test-model', baseUrl: 'http://127.0.0.1',
  hasKey: true, isCustom: true, contextWindow: 128000,
};

const LIST: ModelListItem[] = [
  { id: 'm1', label: '测试模型', model: 'test-model', hasKey: true, compatible: true, active: true },
  { id: 'm2', label: '备用模型', model: 'backup-model', hasKey: true, compatible: true, active: false },
];

function render(ui: React.ReactElement) {
  const container = document.createElement('div');
  document.body.appendChild(container);
  const root: Root = createRoot(container);
  act(() => { root.render(ui); });
  return { container, unmount: () => { act(() => root.unmount()); container.remove(); } };
}

function fireClick(element: Element | null) {
  if (!element) throw new Error('Element not found for click');
  act(() => element.dispatchEvent(new MouseEvent('click', { bubbles: true })));
}

describe('ModelSwitcher', () => {
  beforeEach(() => { document.body.innerHTML = ''; vi.clearAllMocks(); });

  it('shows the active model label', () => {
    const { container, unmount } = render(
      <ModelSwitcher config={CONFIG} modelList={LIST} switchingModel={false} onSwitchModel={vi.fn()} onReconfigure={vi.fn()} />,
    );
    expect(container.textContent).toContain('测试模型');
    unmount();
  });

  it('opens the menu and switches model', () => {
    const onSwitchModel = vi.fn();
    const { container, unmount } = render(
      <ModelSwitcher config={CONFIG} modelList={LIST} switchingModel={false} onSwitchModel={onSwitchModel} onReconfigure={vi.fn()} />,
    );
    fireClick(container.querySelector('.model-switcher button'));
    fireClick(Array.from(container.querySelectorAll('button')).find((b) => b.textContent?.includes('备用模型')) ?? null);
    expect(onSwitchModel).toHaveBeenCalledWith('m2');
    unmount();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/chat/ModelSwitcher.test.tsx`
Expected: FAIL — module not found。

- [ ] **Step 3: Move model menu code from Header into ModelSwitcher**

从 `src/components/chat/Header.tsx` 中把以下内容原样迁移到新组件的 props/实现中（保持 state 名、类名、菜单结构与键盘行为不变）：

- state：`modelMenuOpen`、`modelPresence`、`modelSwitcherRef`、`modelTriggerRef`、`modelFocusOwnedRef`、`retainedModelConfigRef`、`retainedModelListRef`
- 计算：`modelTriggerUsable` 与触发按钮渲染（`model-switcher` 容器 + 当前 label + chevron）
- 菜单：`model-menu` 列表（含 `ModelCard`、`onReconfigure` 入口、`switchingModel` 禁用态、focus/escape 逻辑）

组件签名：

```tsx
interface ModelSwitcherProps {
  config: ConfiguredModel | null;
  modelList: ModelListItem[];
  switchingModel: boolean;
  onSwitchModel: (id: string) => void;
  onReconfigure: () => void;
}

export function ModelSwitcher(props: ModelSwitcherProps) { /* 迁移后的实现 */ }
```

`Header.tsx` 中删除已迁移代码，在原触发按钮位置渲染：

```tsx
<ModelSwitcher
  config={props.config}
  modelList={props.modelList}
  switchingModel={props.switchingModel}
  onSwitchModel={props.onSwitchModel}
  onReconfigure={props.onReconfigure}
/>
```

- [ ] **Step 4: Run tests**

Run: `npx vitest run src/components/chat/ModelSwitcher.test.tsx src/components/chat/Header.test.tsx`
Expected: 新测试 PASS 且 `Header.test.tsx` 原样通过（若个别查询因组件边界变化失败，仅调整测试的查询范围，不改变断言语义）。

- [ ] **Step 5: Commit**

```bash
git add src/components/chat/ModelSwitcher.tsx src/components/chat/ModelSwitcher.test.tsx src/components/chat/Header.tsx
git commit -m "refactor(chat): 抽取 ModelSwitcher 供输入卡复用"
```

---

### Task 5: 审批模式菜单与 InputArea 插槽

**Files:**
- Create: `src/components/chat/ApprovalModeMenu.tsx`
- Test: `src/components/chat/ApprovalModeMenu.test.tsx`
- Modify: `src/components/chat/InputArea.tsx`、`src/components/chat/InputArea.test.tsx`

**Interfaces:**
- Consumes: `ApprovalMode`（`src/lib/navigation.ts`）、`usePresence`。
- Produces:
  - `<ApprovalModeMenu mode onModeChange disabled? />`，根类名 `approval-mode-menu`
  - `InputArea` 新 props：`approvalMode: ApprovalMode`、`onApprovalModeChange: (mode: ApprovalMode) => void`、`modelControl?: ReactNode`
  - `InputArea` 移除 `planMode` / `onPlanToggle` props（计划模式由 `approvalMode === 'plan'` 表达）

- [ ] **Step 1: Write the failing test**

```tsx
// src/components/chat/ApprovalModeMenu.test.tsx
import { act } from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { ApprovalModeMenu } from './ApprovalModeMenu';

function render(ui: React.ReactElement) {
  const container = document.createElement('div');
  document.body.appendChild(container);
  const root: Root = createRoot(container);
  act(() => { root.render(ui); });
  return { container, unmount: () => { act(() => root.unmount()); container.remove(); } };
}

function fireClick(element: Element | null) {
  if (!element) throw new Error('Element not found for click');
  act(() => element.dispatchEvent(new MouseEvent('click', { bubbles: true })));
}

describe('ApprovalModeMenu', () => {
  beforeEach(() => { document.body.innerHTML = ''; vi.clearAllMocks(); });

  it('renders the current mode label', () => {
    const { container, unmount } = render(<ApprovalModeMenu mode="step" onModeChange={vi.fn()} />);
    expect(container.textContent).toContain('逐步批准');
    unmount();
  });

  it('lists all three modes and reports selection', () => {
    const onModeChange = vi.fn();
    const { container, unmount } = render(<ApprovalModeMenu mode="step" onModeChange={onModeChange} />);
    fireClick(container.querySelector('.approval-mode-menu button'));
    const auto = Array.from(container.querySelectorAll('button')).find((b) => b.textContent?.includes('帮我批准')) ?? null;
    fireClick(auto);
    expect(onModeChange).toHaveBeenCalledWith('auto');
    unmount();
  });

  it('closes without reporting when disabled', () => {
    const onModeChange = vi.fn();
    const { container, unmount } = render(<ApprovalModeMenu mode="step" onModeChange={onModeChange} disabled />);
    fireClick(container.querySelector('.approval-mode-menu button'));
    expect(container.querySelectorAll('.approval-mode-menu__item').length).toBe(0);
    unmount();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/chat/ApprovalModeMenu.test.tsx`
Expected: FAIL — module not found。

- [ ] **Step 3: Implement menu and modify InputArea**

```tsx
// src/components/chat/ApprovalModeMenu.tsx
import { useState } from 'react';
import type { ApprovalMode } from '../../lib/navigation';
import { usePresence } from '../../hooks/usePresence';
import { presenceRootProps } from '../../lib/presence-ui';
import { Icon } from '../Icon';

const MODES: Array<{ id: ApprovalMode; label: string; hint: string }> = [
  { id: 'auto', label: '帮我批准', hint: '自动批准工具的每步操作' },
  { id: 'step', label: '逐步批准', hint: '每个工具操作都需要你确认' },
  { id: 'plan', label: '计划模式', hint: '先产出计划，批准后再执行' },
];

interface ApprovalModeMenuProps {
  mode: ApprovalMode;
  onModeChange: (mode: ApprovalMode) => void;
  disabled?: boolean;
}

export function ApprovalModeMenu(props: ApprovalModeMenuProps) {
  const [open, setOpen] = useState(false);
  const presence = usePresence(open, 120);
  const current = MODES.find((item) => item.id === props.mode) ?? MODES[1];

  return (
    <span className="approval-mode-menu">
      <button
        className="approval-mode-menu__trigger"
        type="button"
        aria-haspopup="listbox"
        aria-expanded={open}
        disabled={props.disabled}
        onClick={() => setOpen((value) => !value)}
      >
        <Icon name="shield" size={14} />
        <span>{current.label}</span>
        <Icon name="chevron-down" size={12} />
      </button>
      {presence.mounted && (
        <div className="approval-mode-menu__list" role="listbox" aria-label="审批模式" {...presenceRootProps(presence)}>
          {MODES.map((item) => (
            <button
              key={item.id}
              className={`approval-mode-menu__item${item.id === props.mode ? ' active' : ''}`}
              type="button"
              role="option"
              aria-selected={item.id === props.mode}
              onClick={() => {
                props.onModeChange(item.id);
                setOpen(false);
              }}
            >
              <strong>{item.label}</strong>
              <small>{item.hint}</small>
            </button>
          ))}
        </div>
      )}
    </span>
  );
}
```

修改 `InputArea.tsx`：

- 删除 `planMode` 与 `onPlanToggle` props、移除原 plan toggle 按钮；
- 新增：

```tsx
import type { ReactNode } from 'react';
import { ApprovalModeMenu } from './ApprovalModeMenu';
import type { ApprovalMode } from '../../lib/navigation';

interface InputAreaProps {
  // …现有 props 保留…
  approvalMode: ApprovalMode;
  onApprovalModeChange: (mode: ApprovalMode) => void;
  modelControl?: ReactNode;
}
```

- 在底部操作行渲染（保持既有按钮顺序：附件 → 审批模式 → 模型 → 发送）：

```tsx
<ApprovalModeMenu mode={props.approvalMode} onModeChange={props.onApprovalModeChange} disabled={props.busy} />
{props.modelControl}
```

在 `InputArea.test.tsx` 中把 `planMode` 相关用例改为 `approvalMode="plan"` + `onApprovalModeChange` 断言；新增「渲染审批模式菜单」用例。

- [ ] **Step 4: Run tests**

Run: `npx vitest run src/components/chat/ApprovalModeMenu.test.tsx src/components/chat/InputArea.test.tsx`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/chat/ApprovalModeMenu.tsx src/components/chat/ApprovalModeMenu.test.tsx src/components/chat/InputArea.tsx src/components/chat/InputArea.test.tsx
git commit -m "feat(chat): 审批模式菜单与输入区插槽"
```

---

### Task 6: 新首页 HomeView

**Files:**
- Create: `src/components/home/HomeView.tsx`
- Create: `src/components/home/CapabilityCards.tsx`
- Create: `src/components/home/HomeComposer.tsx`
- Test: `src/components/home/HomeView.test.tsx`

**Interfaces:**
- Consumes: `ApprovalMode`、`AttachmentPicker`、`ApprovalModeMenu`、`ModelSwitcher`（由调用方以 ReactNode 注入）、`ProjectSummary`。
- Produces:
  - `<HomeView … />`（见下签名），根类名 `home-view`
  - `CAPABILITY_CARDS: readonly { id: string; label: string; prompt: string; icon: IconName }[]`
  - `HomeComposer` props：`input busy attachments hasWorkDir projectName branch approvalMode onInputChange onAttachmentsChange onAddPaths onPickAttachments onSend modelControl approvalControl projectControl? onSelectProject? onOpenSettings?`

- [ ] **Step 1: Write the failing test**

```tsx
// src/components/home/HomeView.test.tsx
import { act } from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { beforeEach, describe, expect, it, vi } from 'vitest';
import type { ConfiguredModel, ProjectSummary } from '../../../shared/ipc';
import { HomeView } from './HomeView';

const CONFIG: ConfiguredModel = {
  id: 'm1', label: '本地模型', model: 'local', baseUrl: 'http://127.0.0.1',
  hasKey: true, isCustom: true, contextWindow: 128000,
};

const PROJECTS: ProjectSummary[] = [
  { id: 'p1', name: 'Stellara Work', updatedAt: Date.now(), sessionCount: 2 },
];

const BASE_PROPS = {
  config: CONFIG,
  projects: PROJECTS,
  activeProjectId: 'p1' as string | undefined,
  branch: 'main' as string | null,
  input: '',
  busy: false,
  attachments: [],
  hasWorkDir: true,
  approvalMode: 'step' as const,
  onInputChange: vi.fn(),
  onAttachmentsChange: vi.fn(),
  onAddPaths: vi.fn(),
  onPickAttachments: vi.fn(),
  onSend: vi.fn(),
  onSelectProject: vi.fn(),
  onApprovalModeChange: vi.fn(),
  onSwitchModel: vi.fn(),
  onReconfigure: vi.fn(),
  modelControl: null,
};

function render(ui: React.ReactElement) {
  const container = document.createElement('div');
  document.body.appendChild(container);
  const root: Root = createRoot(container);
  act(() => { root.render(ui); });
  return { container, unmount: () => { act(() => root.unmount()); container.remove(); } };
}

function fireClick(element: Element | null) {
  if (!element) throw new Error('Element not found for click');
  act(() => element.dispatchEvent(new MouseEvent('click', { bubbles: true })));
}

describe('HomeView', () => {
  beforeEach(() => { document.body.innerHTML = ''; vi.clearAllMocks(); });

  it('shows greeting, four capability cards and composer without account UI', () => {
    const { container, unmount } = render(<HomeView {...BASE_PROPS} />);
    expect(container.textContent).toContain('你想让我们在 Stellara Work 中构建什么?');
    expect(container.textContent).toContain('探索并理解代码');
    expect(container.textContent).toContain('构建新功能、应用或工具');
    expect(container.textContent).toContain('审查代码并提出修改建议');
    expect(container.textContent).toContain('修复问题和失败');
    expect(container.textContent).toContain('随心输入');
    expect(container.textContent).not.toMatch(/登录|注册|账户|头像|Plus/);
    unmount();
  });

  it('fills the composer when a capability card is clicked', () => {
    const onInputChange = vi.fn();
    const { container, unmount } = render(<HomeView {...BASE_PROPS} onInputChange={onInputChange} />);
    const card = Array.from(container.querySelectorAll('button')).find((b) => b.textContent?.includes('探索并理解代码')) ?? null;
    fireClick(card);
    expect(onInputChange).toHaveBeenCalledWith(expect.stringContaining('探索'));
    unmount();
  });

  it('shows project and branch chips and hides branch when null', () => {
    const first = render(<HomeView {...BASE_PROPS} />);
    expect(first.container.textContent).toContain('Stellara Work');
    expect(first.container.textContent).toContain('main');
    first.unmount();

    const second = render(<HomeView {...BASE_PROPS} branch={null} />);
    expect(second.container.querySelector('.home-composer__branch')).toBeNull();
    second.unmount();
  });

  it('sends from the composer', () => {
    const onSend = vi.fn();
    const { container, unmount } = render(<HomeView {...BASE_PROPS} input="完成任务" onSend={onSend} />);
    const send = container.querySelector<HTMLButtonElement>('[aria-label="发送"]');
    fireClick(send);
    expect(onSend).toHaveBeenCalled();
    unmount();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/home/HomeView.test.tsx`
Expected: FAIL — module not found。

- [ ] **Step 3: Implement the three components**

```tsx
// src/components/home/CapabilityCards.tsx
import type { IconName } from '../Icon';
import { Icon } from '../Icon';

export const CAPABILITY_CARDS: ReadonlyArray<{ id: string; label: string; prompt: string; icon: IconName }> = [
  { id: 'explore', label: '探索并理解代码', prompt: '请探索并理解当前项目的代码结构与关键实现。', icon: 'search' },
  { id: 'build', label: '构建新功能、应用或工具', prompt: '请帮我构建一个新功能，并说明实现方案。', icon: 'tool' },
  { id: 'review', label: '审查代码并提出修改建议', prompt: '请审查当前代码，指出问题并给出修改建议。', icon: 'shield' },
  { id: 'fix', label: '修复问题和失败', prompt: '请定位并修复当前的问题和失败。', icon: 'alert' },
];

interface CapabilityCardsProps {
  onPick: (prompt: string) => void;
}

export function CapabilityCards({ onPick }: CapabilityCardsProps) {
  return (
    <div className="capability-cards" aria-label="能力入口">
      {CAPABILITY_CARDS.map((card) => (
        <button key={card.id} className="capability-card" type="button" onClick={() => onPick(card.prompt)}>
          <span className="capability-card__icon"><Icon name={card.icon} size={16} /></span>
          <span className="capability-card__label">{card.label}</span>
        </button>
      ))}
    </div>
  );
}
```

```tsx
// src/components/home/HomeComposer.tsx
import { useRef, type ReactNode } from 'react';
import type { AttachmentMeta } from '../../../shared/ipc';
import { AttachmentPicker } from '../attachments/AttachmentPicker';
import { ApprovalModeMenu } from '../chat/ApprovalModeMenu';
import type { ApprovalMode } from '../../lib/navigation';
import { Icon } from '../Icon';

interface HomeComposerProps {
  input: string;
  busy: boolean;
  attachments: AttachmentMeta[];
  hasWorkDir: boolean;
  projectName?: string;
  branch: string | null;
  approvalMode: ApprovalMode;
  modelControl?: ReactNode;
  projectControl?: ReactNode;
  onInputChange: (value: string) => void;
  onAttachmentsChange: (next: AttachmentMeta[]) => void;
  onAddPaths: (paths: string[]) => void;
  onPickAttachments: () => void;
  onSend: (returnFocus?: HTMLElement | null) => void;
  onApprovalModeChange: (mode: ApprovalMode) => void;
}

export function HomeComposer(props: HomeComposerProps) {
  const textareaRef = useRef<HTMLTextAreaElement>(null);

  return (
    <div className="home-composer">
      <div className="home-composer__chips">
        {props.projectControl ?? (
          <span className="home-composer__chip home-composer__project">
            <Icon name="folder" size={13} />
            {props.projectName ?? '选择项目'}
          </span>
        )}
        <span className="home-composer__chip home-composer__target">
          <Icon name="server" size={13} />
          本地
        </span>
        {props.branch !== null && (
          <span className="home-composer__chip home-composer__branch">
            <Icon name="file-tree" size={13} />
            {props.branch}
          </span>
        )}
      </div>
      <textarea
        ref={textareaRef}
        rows={3}
        value={props.input}
        disabled={props.busy}
        aria-label="输入任务"
        placeholder="随心输入"
        onChange={(event) => props.onInputChange(event.target.value)}
        onKeyDown={(event) => {
          if (event.key === 'Enter' && (event.ctrlKey || event.metaKey)) {
            event.preventDefault();
            props.onSend(event.currentTarget);
          }
        }}
      />
      <div className="home-composer__footer">
        <AttachmentPicker
          attachments={props.attachments}
          onAttachmentsChange={props.onAttachmentsChange}
          onPick={props.onPickAttachments}
          onAddPaths={props.onAddPaths}
          disabled={props.busy || !props.hasWorkDir}
        />
        <ApprovalModeMenu mode={props.approvalMode} onModeChange={props.onApprovalModeChange} disabled={props.busy} />
        {props.modelControl}
        <button
          className="home-composer__send"
          type="button"
          aria-label="发送"
          disabled={props.busy || !props.input.trim()}
          onClick={(event) => props.onSend(event.currentTarget)}
        >
          <Icon name="send" size={15} />
        </button>
      </div>
    </div>
  );
}
```

```tsx
// src/components/home/HomeView.tsx
import type { ConfiguredModel, ProjectSummary, AttachmentMeta } from '../../../shared/ipc';
import type { ApprovalMode } from '../../lib/navigation';
import { CapabilityCards } from './CapabilityCards';
import { HomeComposer } from './HomeComposer';
import { Icon } from '../Icon';
import type { ReactNode } from 'react';

interface HomeViewProps {
  config: ConfiguredModel | null;
  projects: ProjectSummary[];
  activeProjectId?: string;
  branch: string | null;
  input: string;
  busy: boolean;
  attachments: AttachmentMeta[];
  hasWorkDir: boolean;
  approvalMode: ApprovalMode;
  modelMissing?: boolean;
  modelControl?: ReactNode;
  projectControl?: ReactNode;
  onOpenSettings?: () => void;
  onInputChange: (value: string) => void;
  onAttachmentsChange: (next: AttachmentMeta[]) => void;
  onAddPaths: (paths: string[]) => void;
  onPickAttachments: () => void;
  onSend: (returnFocus?: HTMLElement | null) => void;
  onApprovalModeChange: (mode: ApprovalMode) => void;
}

export function HomeView(props: HomeViewProps) {
  const activeProject = props.projects.find((project) => project.id === props.activeProjectId);

  return (
    <main className="home-view" aria-labelledby="home-title">
      {props.modelMissing && (
        <div className="no-model-banner" role="alert">
          <Icon name="alert" size={15} />
          <span>尚未配置模型，Agent 暂时无法执行任务。</span>
          <button className="no-model-banner__btn no-model-banner__btn--settings" type="button" onClick={props.onOpenSettings}>
            去设置
          </button>
        </div>
      )}
      <div className="home-view__center">
        <span className="home-view__glyph" aria-hidden="true"><Icon name="terminal" size={22} /></span>
        <h1 id="home-title">你想让我们在 Stellara Work 中构建什么?</h1>
        <CapabilityCards onPick={props.onInputChange} />
      </div>
      <HomeComposer
        input={props.input}
        busy={props.busy}
        attachments={props.attachments}
        hasWorkDir={props.hasWorkDir}
        projectName={activeProject?.name}
        branch={props.branch}
        approvalMode={props.approvalMode}
        modelControl={props.modelControl}
        projectControl={props.projectControl}
        onInputChange={props.onInputChange}
        onAttachmentsChange={props.onAttachmentsChange}
        onAddPaths={props.onAddPaths}
        onPickAttachments={props.onPickAttachments}
        onSend={props.onSend}
        onApprovalModeChange={props.onApprovalModeChange}
      />
    </main>
  );
}
```

- [ ] **Step 4: Run test**

Run: `npx vitest run src/components/home/HomeView.test.tsx && npm run typecheck`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/home/
git commit -m "feat(home): 新首页与输入卡"
```

---

### Task 7: 占位页组件

**Files:**
- Create: `src/components/shell/PlaceholderPage.tsx`
- Test: `src/components/shell/PlaceholderPage.test.tsx`

**Interfaces:**
- Consumes: `Icon`、`IconName`。
- Produces: `<PlaceholderPage title description icon onBackHome? />`，根类名 `placeholder-page`。

- [ ] **Step 1: Write the failing test**

```tsx
// src/components/shell/PlaceholderPage.test.tsx
import { act } from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { PlaceholderPage } from './PlaceholderPage';

function render(ui: React.ReactElement) {
  const container = document.createElement('div');
  document.body.appendChild(container);
  const root: Root = createRoot(container);
  act(() => { root.render(ui); });
  return { container, unmount: () => { act(() => root.unmount()); container.remove(); } };
}

describe('PlaceholderPage', () => {
  beforeEach(() => { document.body.innerHTML = ''; });

  it('renders title and description', () => {
    const { container, unmount } = render(
      <PlaceholderPage title="Pull Request" description="Pull Request 将在后续版本推出" />,
    );
    expect(container.textContent).toContain('Pull Request');
    expect(container.textContent).toContain('将在后续版本推出');
    unmount();
  });

  it('calls back-home action when provided', () => {
    const onBackHome = vi.fn();
    const { container, unmount } = render(
      <PlaceholderPage title="已安排" description="即将推出" onBackHome={onBackHome} />,
    );
    act(() => {
      container.querySelector('button')?.dispatchEvent(new MouseEvent('click', { bubbles: true }));
    });
    expect(onBackHome).toHaveBeenCalledTimes(1);
    unmount();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/components/shell/PlaceholderPage.test.tsx`
Expected: FAIL — module not found。

- [ ] **Step 3: Implement**

```tsx
// src/components/shell/PlaceholderPage.tsx
import { Icon, type IconName } from '../Icon';

interface PlaceholderPageProps {
  title: string;
  description: string;
  icon?: IconName;
  onBackHome?: () => void;
}

export function PlaceholderPage({ title, description, icon = 'list', onBackHome }: PlaceholderPageProps) {
  return (
    <main className="placeholder-page" data-motion="page-enter">
      <span className="placeholder-page__icon"><Icon name={icon} size={20} /></span>
      <h1>{title}</h1>
      <p>{description}</p>
      {onBackHome && (
        <button className="btn btn-secondary" type="button" onClick={onBackHome}>
          返回首页
        </button>
      )}
    </main>
  );
}
```

- [ ] **Step 4: Run test**

Run: `npx vitest run src/components/shell/PlaceholderPage.test.tsx`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/shell/PlaceholderPage.tsx src/components/shell/PlaceholderPage.test.tsx
git commit -m "feat(shell): 占位页组件"
```

---

### Task 8: 侧栏信息架构重构

**Files:**
- Modify: `src/components/Sidebar.tsx`（导航区、最近分组、底部工具区）
- Modify: `src/components/Sidebar.test.tsx`

**Interfaces:**
- Consumes: `AppSection`（`src/lib/navigation.ts`）、现有 props。
- Produces: Sidebar 新 props：
  - `activeSection?: AppSection`
  - `onNavigate: (section: 'home' | 'tasks' | 'memory' | 'files' | 'pull-requests' | 'scheduled') => void`（替换 `onNavigateHome/Projects/Tasks/Memory/Files`）
  - `onNew: (returnFocus?: HTMLElement | null) => void`（现有，用于「新对话」）
  - 其余项目/会话管理 props 不变

- [ ] **Step 1: Update tests first**

在 `Sidebar.test.tsx` 中：

- 所有 `onNavigateHome/onNavigateProjects/onNavigateTasks/onNavigateMemory/onNavigateFiles` mock 替换为 `onNavigate: vi.fn()`；
- 断言主导航按钮为「新对话」「Pull Request」「已安排」，且**不存在**「插件」；
- 新增：`onNavigate` 在点击 Pull Request / 已安排时收到 `'pull-requests'` / `'scheduled'`；
- 新增：底部工具区包含「记忆」「文件」「设置」；点击「记忆」调用 `onNavigate('memory')`，点击「设置」调用 `onOpenSettings`；
- 新增「最近」分组：无项目会话（`projectId` 为空）按 `updatedAt` 倒序展示最多 5 条，点击调用 `onSelect`；为空时不渲染分组标题。

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/components/Sidebar.test.tsx`
Expected: FAIL（新断言与 props 不匹配）。

- [ ] **Step 3: Refactor Sidebar render**

替换 `sidebar-primary` 区块为：

```tsx
      <nav className="sidebar-primary" aria-label="主要导航">
        <button className="sidebar-primary-item" type="button" onClick={(event) => onNew(event.currentTarget)}>
          <Icon name="plus" size={15} />
          <span>新对话</span>
        </button>
        <button
          className={`sidebar-primary-item${activeSection === 'pull-requests' ? ' sidebar-primary-item--active' : ''}`}
          type="button"
          aria-current={activeSection === 'pull-requests' ? 'page' : undefined}
          onClick={() => onNavigate('pull-requests')}
        >
          <Icon name="copy" size={15} />
          <span>Pull Request</span>
        </button>
        <button
          className={`sidebar-primary-item${activeSection === 'scheduled' ? ' sidebar-primary-item--active' : ''}`}
          type="button"
          aria-current={activeSection === 'scheduled' ? 'page' : undefined}
          onClick={() => onNavigate('scheduled')}
        >
          <Icon name="calendar" size={15} />
          <span>已安排</span>
        </button>
      </nav>
```

在项目组之后、`sidebar-search`/`session-list` 结构不变的前提下，加入「最近」分组（数据取 `sessions.filter((s) => !s.projectId).sort((a, b) => b.updatedAt - a.updatedAt).slice(0, 5)`，为空不渲染）：

```tsx
      {recentSessions.length > 0 && (
        <section className="sidebar-recent" aria-label="最近">
          <h2 className="sidebar-section-title">最近</h2>
          <ul className="session-list">
            {recentSessions.map((session) => (
              <li key={session.id}>{renderSession(session)}</li>
            ))}
          </ul>
        </section>
      )}
```

在 `<aside>` 末尾加底部工具区：

```tsx
      <div className="sidebar-tools">
        <button className="sidebar-tool" type="button" onClick={() => onNavigate('memory')}>
          <Icon name="database" size={15} />
          <span>记忆</span>
        </button>
        <button className="sidebar-tool" type="button" onClick={() => onNavigate('files')}>
          <Icon name="file-tree" size={15} />
          <span>文件</span>
        </button>
        <button className="sidebar-tool" type="button" onClick={onOpenSettings}>
          <Icon name="settings" size={15} />
          <span>设置</span>
        </button>
      </div>
```

删除旧的「首页/项目/工作记录/记忆/文件/设置」主导航与 `onNavigateHome` 等 props。

- [ ] **Step 4: Run test**

Run: `npx vitest run src/components/Sidebar.test.tsx`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/Sidebar.tsx src/components/Sidebar.test.tsx
git commit -m "feat(shell): 侧栏 IA 重构（新对话/PR/已安排/最近/工具区）"
```

---

### Task 9: MainView 集成与新壳切换

**Files:**
- Modify: `src/components/MainView.tsx`
- Modify: `src/components/MainView.test.tsx`
- Modify: `src/App.tsx`、`src/App.test.tsx`（仅在 props/断言受影响处）
- Delete: `src/components/chat/Header.tsx`、`src/components/chat/Header.test.tsx`
- Delete: `src/components/HomeDashboard.tsx`、`src/components/HomeDashboard.test.tsx`

**Interfaces:**
- Consumes: `useNavHistory`、`AppTopBar`、`HomeView`、`PlaceholderPage`、`ModelSwitcher`、`ApprovalModeMenu`、`app.getGitBranch`。
- Produces: MainView 使用 `nav.current.section` 渲染：`home` → HomeView；`tasks` → ChatStream + InputArea；`memory` → MemoryCenter；`files` → SidebarFileView；`pull-requests` / `scheduled` → PlaceholderPage。

- [ ] **Step 1: Update MainView tests for new structure**

在 `MainView.test.tsx` 中：

- 移除对 `Header` / `HomeDashboard` 类名（`main-header`、`dashboard-composer`）的断言，替换为新类名（`app-topbar`、`home-view`、`home-composer`）；
- 新增：初始渲染包含问候语「你想让我们在 Stellara Work 中构建什么?」；
- 新增：点击能力卡「探索并理解代码」后，输入框值被预填；
- 新增：后退/前进按钮在历史为空时 disabled，进入会话后「后退」可用且点击回到首页；
- 新增：点击侧栏「Pull Request」渲染占位页文案「Pull Request 将在后续版本推出」；
- 新增：审批模式切到「帮我批准」后，收到的 `pendingApproval` 会立即调用 `window.electronAPI.chat.approve(id, true)`（用已 mock 的 electronAPI 断言）；
- 保留既有会话切换/发送/审批/文件树/记忆等行为断言（仅更新查询选择器）。

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/components/MainView.test.tsx`
Expected: FAIL。

- [ ] **Step 3: Rewire MainView**

关键改动：

```tsx
import { useNavHistory } from '../hooks/useNavHistory';
import { INITIAL_NAV, type ApprovalMode, type AppSection } from '../lib/navigation';
import { AppTopBar } from './shell/AppTopBar';
import { HomeView } from './home/HomeView';
import { PlaceholderPage } from './shell/PlaceholderPage';
import { ModelSwitcher } from './chat/ModelSwitcher';
```

状态替换：

```tsx
  // 删除 const [activeSection, setActiveSection] = useState<...>('home')
  const nav = useNavHistory(INITIAL_NAV);
  const activeSection: AppSection = nav.current.section;
  const [approvalMode, setApprovalMode] = useState<ApprovalMode>('step');
  const planMode = approvalMode === 'plan';
  const [branch, setBranch] = useState<string | null>(null);

  useEffect(() => {
    let active = true;
    if (!activeWorkDir) { setBranch(null); return; }
    void window.electronAPI.app.getGitBranch(activeWorkDir).then((value) => {
      if (active) setBranch(value);
    });
    return () => { active = false; };
  }, [activeWorkDir]);

  // 帮我批准：审批事件到达时自动放行
  useEffect(() => {
    if (approvalMode !== 'auto' || !pendingApproval) return;
    window.electronAPI.chat.approve(pendingApproval.id, true);
    setPendingApproval(null);
  }, [approvalMode, pendingApproval]);
```

导航函数替换：

```tsx
  function navigateToSection(next: AppSection, sessionId: string | null = null) {
    nav.push({ section: next, sessionId });
  }
```

会话选择：

```tsx
  function handleSelectSession(id: string) {
    nav.push({ section: 'tasks', sessionId: id });
    // …保留原有 onSessionSwitched 等逻辑…
  }
```

渲染骨架：

```tsx
  return (
    <div className="main-view">
      <AppTopBar
        canGoBack={nav.canGoBack}
        canGoForward={nav.canGoForward}
        onBack={nav.back}
        onForward={nav.forward}
        sidebarOpen={sidebarOpen}
        workspaceOpen={props.workspaceOpen}
        onToggleSidebar={onToggleSidebar}
        onToggleWorkspace={props.onToggleWorkspace}
        executionTarget={undefined /* Plan 2 注入服务器选择器 */}
      />
      <div className="main-layout">
        {sidebarPresence.mounted && (
          <Sidebar
            ref={undefined}
            presence={sidebarPresence}
            projects={projects}
            sessions={sessions}
            activeId={activeSessionId}
            mode={workspaceMode === 'tabs' ? 'compact' : 'full'}
            activeSection={activeSection}
            onNavigate={navigateToSection}
            onOpenSettings={() => onOpenSettings()}
            onNew={(returnFocus) => void handleNewSession(undefined, returnFocus)}
            onSelect={handleSelectSession}
            /* …其余管理 props 原样保留… */
          />
        )}
        <div className="main-content">
          {activeSection === 'tasks' ? (
            /* …TabBar / BrowserTab / ChatStream 原样保留… */
            <InputArea
              approvalMode={approvalMode}
              onApprovalModeChange={setApprovalMode}
              modelControl={
                <ModelSwitcher
                  config={config}
                  modelList={modelList}
                  switchingModel={switchingModel}
                  onSwitchModel={(id) => void handleSwitchModel(id)}
                  onReconfigure={onReconfigure}
                />
              }
              /* …其余原 props… */
            />
          ) : activeSection === 'memory' ? (
            <MemoryCenter />
          ) : activeSection === 'files' ? (
            <SidebarFileView workDir={activeWorkDir ?? null} onOpenFullScreen={() => { openFileTree(); }} />
          ) : activeSection === 'pull-requests' ? (
            <PlaceholderPage title="Pull Request" description="Pull Request 将在后续版本推出" icon="copy" onBackHome={() => navigateToSection('home')} />
          ) : activeSection === 'scheduled' ? (
            <PlaceholderPage title="已安排" description="定时任务将在后续版本推出" icon="calendar" onBackHome={() => navigateToSection('home')} />
          ) : (
            <HomeView
              config={config}
              projects={projects}
              activeProjectId={activeProject?.id}
              branch={branch}
              input={input}
              busy={busy}
              attachments={attachments}
              hasWorkDir={!!activeWorkDir}
              approvalMode={approvalMode}
              modelMissing={!config}
              onOpenSettings={() => onOpenSettings()}
              onInputChange={setInput}
              onAttachmentsChange={setAttachments}
              onAddPaths={(paths) => void handleAddAttachmentPaths(paths)}
              onPickAttachments={() => void handlePickAttachmentFiles()}
              onSend={(returnFocus) => void handleSend(returnFocus)}
              onApprovalModeChange={setApprovalMode}
              modelControl={
                <ModelSwitcher
                  config={config}
                  modelList={modelList}
                  switchingModel={switchingModel}
                  onSwitchModel={(id) => void handleSwitchModel(id)}
                  onReconfigure={onReconfigure}
                />
              }
            />
          )}
        </div>
      </div>
      {/* …对话框/文件树/工作区面板等原样保留… */}
    </div>
  );
```

注意：
- `handleSend` / `handleNewSession` 若签名无 `returnFocus`，为其补上可选参数并透传（现有 HomeDashboard 已按此调用）。
- `handleNewSession` 内部切到 `tasks` 时改用 `nav.push`；已存在会话 id 时 `nav.push({ section: 'tasks', sessionId: created.id })`。
- 删除 `onNavigateProjects` 相关分支与 `activeSection === 'projects'` 渲染。
- 删除 `Header`、`HomeDashboard` import 与文件。

- [ ] **Step 4: Run full renderer tests and typecheck**

Run: `npm run typecheck && npx vitest run src/components/MainView.test.tsx src/App.test.tsx`
Expected: PASS。再跑 `npm test` 确认无其他回归（MemoryCenter、FileTreeModal、BrowserTab 等）。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(shell): MainView 接入顶栏/首页/占位页并移除旧 Header 与首页"
```

---

### Task 10: 设计令牌与外壳样式

**Files:**
- Modify: `src/styles/tokens.css`
- Modify: `src/styles/workbench.css`
- Modify: `src/styles/global.css`（仅必要的全局规则）
- Test: `src/styles/tokens.test.ts`、`src/styles/motion-contract.test.ts`（仅在令牌断言受影响处更新）

**Interfaces:**
- Consumes: 无。
- Produces: 新外壳类名样式：`.app-topbar`、`.home-view`、`.capability-cards`、`.home-composer`、`.execution-target-chip`、`.approval-mode-menu`、`.sidebar-tools`、`.sidebar-recent`、`.placeholder-page`。

- [ ] **Step 1: Add shell tokens**

在 `tokens.css` 的 `:root` 中加入：

```css
  --topbar-height: 44px;
  --color-bg-topbar: #f8f8f5;
  --color-bg-composer: #ffffff;
  --color-chip-bg: #f0f0ec;
  --radius-card: 12px;
```

`[data-theme="dark"]` 中对应加入：

```css
  --color-bg-topbar: #20201e;
  --color-bg-composer: #2a2a27;
  --color-chip-bg: #30302d;
```

- [ ] **Step 2: Update layout styles**

在 `workbench.css` 中：

- `.main-view` 网格行改为 `var(--topbar-height) minmax(0, 1fr)`；
- 新增 `.app-topbar`（高度、拖拽区、左右布局、macOS 红绿灯 padding 与现有 `.main-header` 规则一致）：

```css
.app-topbar {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  height: var(--topbar-height);
  padding: 0 var(--space-3);
  background: var(--color-bg-topbar);
  border-bottom: 1px solid var(--color-border);
  -webkit-app-region: drag;
  z-index: var(--z-header);
}

html[data-platform='darwin'] .app-topbar {
  padding-left: 78px;
}

.app-topbar button,
.app-topbar [role='menu'],
.app-topbar [role='listbox'] {
  -webkit-app-region: no-drag;
}

.app-topbar__nav,
.app-topbar__actions {
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

.app-topbar__center {
  flex: 1;
}

.execution-target-chip {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  height: 26px;
  padding: 0 var(--space-2);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  color: var(--color-text-soft);
  font-size: var(--fs-sm);
}

.execution-target-chip i {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-success);
}
```

- 新增首页与能力卡样式（居中列、卡片 4 列自适应、输入卡浮层）：

```css
.home-view {
  display: grid;
  grid-template-rows: minmax(0, 1fr) auto;
  align-items: center;
  gap: var(--space-6);
  padding: var(--space-7) var(--space-6) var(--space-5);
  overflow: auto;
}

.home-view__center {
  display: grid;
  justify-items: center;
  gap: var(--space-5);
  max-width: 880px;
  margin: 0 auto;
  text-align: center;
}

.home-view__glyph {
  display: inline-flex;
  padding: var(--space-3);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-card);
  color: var(--color-text-soft);
}

.capability-cards {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: var(--space-3);
  width: 100%;
}

.capability-card {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  min-height: 96px;
  padding: var(--space-4);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-card);
  background: var(--color-bg-content);
  text-align: left;
}

.home-composer {
  width: min(880px, 100%);
  margin: 0 auto;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-card);
  background: var(--color-bg-composer);
  box-shadow: var(--shadow-md);
}

.home-composer textarea {
  width: 100%;
  border: 0;
  outline: none;
  resize: none;
  background: transparent;
  padding: var(--space-4);
  font: inherit;
}
```

- 需保留现有 `main-header` 样式到 Task 9 删除 Header 后再清理（若删除后无引用，一并移除）。
- `.sidebar-tools` 固定在侧栏底部（`margin-top: auto`），`.sidebar-recent` 与项目分组风格一致；`.placeholder-page` 居中空状态。

- [ ] **Step 3: Update token tests**

运行 `npx vitest run src/styles/tokens.test.ts src/styles/motion-contract.test.ts`。若有断言因新增/调整令牌失败，按新值为准更新（如 `--topbar-height`、`--radius-card` 加入允许清单）；motion contract 不允许新增动画时长，保持既有动效令牌不变。

- [ ] **Step 4: Full verification**

Run: `npm run typecheck && npm test && npm run build`
Expected: 全部通过。

- [ ] **Step 5: Manual smoke**

Run: `npm run dev`，人工确认：顶栏拖拽/红绿灯不重叠；侧栏新导航与项目分组可用；首页问候语、四张卡片、chips、发送可用；进入会话后对话/审批/计划/中止不回归；暗色主题正常。

- [ ] **Step 6: Commit**

```bash
git add src/styles/ src/components/
git commit -m "style(shell): 0.9.3 设计令牌与外壳视觉"
```

---

## Self-Review

**Spec coverage（本计划对应 spec 第 3 节与本计划 Global Constraints）：**
- 3.1 窗口外壳与顶栏 → Task 2、9（执行端选择器为插槽，Plan 2 注入）。
- 3.2 侧栏 IA → Task 8（通知图标仅聚合调度结果，Plan 3 接入，本计划不渲染假数据）。
- 3.3 首页 → Task 6（执行端 chip 静态「本地」，Plan 2 改为联动）。
- 3.4 会话视图 → Task 5、9、10（对话组件逻辑保留、样式重写）。
- 3.5 设置面板皮肤 → Task 10（新增「服务器」分组在 Plan 2）。
- 3.6 PR 占位 → Task 7、9；已安排占位 → Task 7、9（Plan 3 替换为真页面）。
- 3.7 设计令牌 → Task 10。
- 麦克风不放、无账号 UI → Task 6/9 测试断言覆盖。

**Placeholder scan:** 无 TBD/TODO；所有步骤含具体代码或精确修改指令。

**Type consistency:** `AppSection`、`NavState`、`ApprovalMode`、`useNavHistory` 返回值、`AppTopBar` props、`HomeView`/`HomeComposer` props、`ModelSwitcher` props、`ApprovalModeMenu` props、`PlaceholderPage` props、`app.getGitBranch` 签名在各任务间一致。
