# S4 审批体验 / S5 定时任务写策略 — 真机冒烟记录

- 日期：2026-09-22
- 代码版本：main `900edc7`（含 Anthropic 流式对齐合并）
- 方式：独立 `--user-data-dir` 临时 profile（不触碰真实数据）；真实 DeepSeek 模型；渲染层经 CDP 驱动真实 UI（点击/输入与人工一致）；密钥经 Electron safeStorage 在应用进程内解密，未打印、未落盘
- 结论：**全部通过，未发现需要修复的代码问题**

## S5 行为冒烟（4/4）

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| 1 | `write_file` + `fileScopes: src/**` 写入 `src/allowed.txt` | ✅ | 文件实际写入；模型执行了读后复核 |
| 2 | 根目录写入（范围外） | ✅ | 模型回传「路径 root-denied.txt 超出任务声明的可写范围（已拒绝）」；文件不存在 |
| 3 | 命令白名单 `npm test` 放行 / `git init` 拒绝 | ✅ | 测试脚本产物 `cmd-ok.txt` 存在；`.git` 不存在 |
| 4 | 无策略任务 | ✅ | 写工具不可见（模型报告没有 `write_file`）；只读读取正常 |

四个 run 状态均 `success`，无 error。

## S5 UI 目检（4/4）

| # | 检查点 | 结果 |
|---|---|---|
| 1 | 任务卡摘要「写操作：写入文件、编辑文件 · 范围 src/**」 | ✅ |
| 2 | 编辑器回填：勾选状态、可写范围、工作目录 | ✅ |
| 3 | 保存风险确认（原生弹窗） | ✅ 原文：「该任务将在无人值守时执行写操作（按声明范围）。确认按最小范围授权？」 |
| 4 | 保存后卡片摘要保持 | ✅ |

## S4 审批体验目检（5/5）

| # | 检查点 | 结果 |
|---|---|---|
| 1 | 工具审批卡倒计时（剩余 60s） | ✅ |
| 2 | 大参数折叠：1201 字符 → 「展开全部（1544 字符）」→ 收起 | ✅ |
| 3 | 超时反馈：「已超时，自动拒绝」+ 按钮禁用 + 1.5s 后自动清理 | ✅ |
| 4 | 计划卡倒计时（剩余 299s）与按钮 | ✅ |
| 5 | 计划拒绝：显示「[计划未批准]」，输入区恢复空闲 | ✅ |

`subagentId` 字段化未做真机触发（需子代理链路），由 `src/components/ApprovalTopBar.test.tsx` 单测覆盖。

## 备注

- 验证截图仅作会话内目检，未入库（临时文件已删除）。复跑方法：以独立 `--user-data-dir` 启动 `NODE_ENV=production npx electron . --remote-debugging-port=9222`，用 CDP 驱动 UI；定时任务行为可经 `window.electronAPI.scheduled.*` 走与 UI 相同路径。
- 相关设计/计划：`docs/superpowers/specs/2026-09-20-s5-scheduled-write-policy-design.md`、`docs/superpowers/plans/2026-09-20-s5-scheduled-write-policy.md`。
